# 2026-09-17

## 10:00 知识库嵌入模型切到智谱云端（去掉本地 Ollama 依赖）

起因：用户问「当前项目还需要用本地模型吗」。结论是——本地模型（Ollama + bge-m3）**只**用于知识库嵌入，
对话 LLM 本来就走 DeepSeek 云端。用户随即给出智谱 API Key，要求「改云端」。

### 做了什么

1. **装智谱插件**：Dify 1.10 原本只装了 `langgenius/ollama` 与 `langgenius/deepseek`。
   容器能出网（`curl marketplace.dify.ai` 返回 200），走正规通道
   `POST /console/api/workspaces/current/plugin/install/marketplace`，
   装上 `langgenius/zhipuai:0.0.35`（宿主另外留了 `.difypkg` 备份在 `C:\Users\Public\uas_doc\`）。
2. **写凭据**：`POST .../model-providers/langgenius/zhipuai/zhipuai/credentials` → 201。
   之后可用的 embedding 模型：`embedding-2` / `embedding-3` / `text_embedding`。
3. **切库**：对两个知识库 `PATCH /console/api/datasets/<id>`：

   ```json
   {"indexing_technique":"high_quality",
    "embedding_model":"embedding-3",
    "embedding_model_provider":"langgenius/zhipuai/zhipuai"}
   ```

   | 库 | dataset_id | 文档数 |
   |---|---|---|
   | 留学机构业务知识库 | `4d1d8994-c9ca-4d2f-b9dd-4ba56bbcf338` | 26 |
   | AI课程知识库 | `6292a6e5-cc91-4fd4-a878-383e5a4c641b` | 3 |

### 关键发现（可复用）

- **Dify 允许改「已建好的」知识库的嵌入模型，并会自动重建索引**：
  `DatasetService._update_internal_dataset()` → `_handle_indexing_technique_change()` → 改完调用
  `deal_dataset_vector_index_task.delay(dataset.id, action)`。
  ⇒ 不必新建库、不必重传文档；**dataset_id 不变 ⇒ Chatflow 的 knowledge-retrieval 节点无需改**。
- ⚠️ PATCH body **必须带 `indexing_technique`**：`_handle_indexing_technique_change` 直接读
  `data["indexing_technique"]`，不带就 KeyError → 500。
- 智谱 `embedding-3` 维度 **2048**（bge-m3 是 1024）⇒ 换模型等于换向量空间，**必须重建索引**。
- Weaviate 集合名只跟 dataset_id 有关（`Vector_index_<dataset_id>_Node`），**不会随模型重建**；
  所以「维度到底换没换」只能靠**检索能不能命中**来验证（维度不匹配时向量检索会报错）。
- 验证手段：`POST /console/api/datasets/<id>/hit-testing`，看返回的 `records` 条数与 score。

### 验证结果（全绿）

- 两库文档索引状态：`completed`（26 / 3）。
- Weaviate 对象数：业务库 26、课程库 74。
- hit-testing 三个 query（签证 / 申请服务 / 公司介绍）各命中 4 条，score 0.45–0.56，内容相关。
- 容器到 `open.bigmodel.cn` 可达（空请求返回 401，符合预期）。
- ⇒ **Ollama 已无任何引用，可以停**；`langgenius/ollama` 插件保留作回退。

### 脚本沉淀（`C:\Users\Public\uas_doc\`）

`_probe_zhipu.py`、`_probe_plugins.py`、`_install_zhipu.py`、`_set_zhipu_cred.py`、
`_switch_kb_embed_cloud.py`、`_verify_kb_cloud.py`。

### 文档同步

- `reports/验收基线汇总.md`：`high_quality + bge-m3` → `high_quality + embedding-3（智谱云端）`。
- `04-项目记忆/MEMORY.md`（工作区 + 交付包）：本地模型条目改为「已停用」。
- 技术文档 md 源 / docx / `.env` **均未提 bge-m3**，无需改动。

### ⚠️ 安全

智谱 Key 由用户在对话里**明文提供**，已建议轮换。该 Key 只写入 Dify 的 provider 凭据（存 Dify 自己的库），
**未落到任何项目文件**。

### 未决

- 对话 LLM 目前仍是 DeepSeek（云端）。用户若想统一到智谱，需要逐个应用改 model-config（7 个应用），
  属较大改动，待确认。

---

## 11:10 回填交付包（技术文档 + 技能）

用户指示「更新项目，技术文档和skill」。上面那行「技术文档均未提 bge-m3 ⇒ 无需改动」只对了一半：
文档确实没写嵌入模型，**正因为没写，读者无法判断是否需要本地模型**，所以要主动补上模型拓扑。

### 技术文档（md 源 + 两份 docx）

- `docs/tech-doc-pipeline/stage1/final_draft.md`
  - §5.3 新增段落「**模型接入：对话与嵌入均走云端**」（对话走云端大模型 / 嵌入走智谱 `embedding-3` 2048 维；
    全链路不再依赖本地常驻模型；代价是建索引与推理需出网）。
  - §5.3 新增段落「**知识库与嵌入模型**」（换模型=换向量空间必须整体重建；PATCH 必带 `indexing_technique`
    否则 500；验证必须用真实检索命中，不能只看配置回显）。
  - 附录 B 新增行「模型接入」；§9 限制 1 的「常驻模型服务」→「云端模型服务」。
- **两份 docx**（`01-需求与技术文档/` + `docs/`）用 `C:\Users\Public\uas_doc\_patch_cloud_embed_docx.py` 打补丁，
  md5 一致 = `398c206d…`（75515 B）。除新增内容外，还**顺手修掉了上一轮 docx 补丁的排版回退**：
  §8.1「未覆盖说明」与 §9 限制 1 两段曾被 `set_all` 压成**整段粗体**，现拆回「粗体引导句 + 正文」；
  附录 B 表也从 12 行按 md 重建为 **18 行**（原表缺 5 行、且「错误码数」错写 7 个）。

### 技能

- **新建 `dify-kb-embedding-switch`**（用户级 + 交付包 `.workbuddy/skills/` 同步）：
  三条硬事实（换模型=换向量空间 / `dataset_id` 不变⇒检索节点零改动 / 平台自动重建）、
  五个坑（漏 `indexing_technique`→500、插件未装、凭据未写、只看回显、Weaviate 403）、
  六步操作（含全部控制面端点）、回滚、以及「什么时候不该换」。
- 自带脚本 `scripts/kb_embed_switch.py`（纯标准库，凭据走环境变量，默认 dry-run）。
  **已在真实 Dify 上全链路验证**：登录 → 读现状 → `PATCH` 200 → 文档 `completed` → hit-testing 命中 4 条（score 0.52）。
  验证中发现并修掉一个真 bug：数据集级 `indexing_status` 在稳定态是 `None`，
  拿它当判据会误判超时 ⇒ 改为看**文档级**状态（无进行中 + 至少一条 completed）。
- `docs/SKILLS.md`：索引表加行、二级标题数量 4→6（原本就与正文的 5 节不一致）、新增 §2.6、加 09-17 回填说明。

### 备注

- 起本机 Dify 前要 `docker desktop start`，引擎就绪约 50 秒（本次实测）。
- `_lab/tmp/launcher-verify/*` 里还有 Ollama 探测残留，属临时目录，未动。

---

## 11:30 处理「12 都改」：重跑 Stage2/Stage3 + 清理 .bak

### 第 1 项：重跑中间产物（md → stage2 HTML → docx）

**新发现（比预想严重）**：交付包里的中间产物是**三方不一致**，不只是 stage2 过期——
md §5.8–5.11 与 docx §5.8–5.11 是**两版不同草稿**，stage2 HTML 干脆整段缺失。
其中 docx 那版有一处**事实错误**：声称 `pdf` 出口"自研 pdf_writer **内嵌 CJK 字体**"，
而 md 写的是"**不嵌字体文件**，实测 Edge/Chrome 的 PDFium 中文渲染成乱码"（与代码一致）。
⇒ 以 **md 为准**（既更新也更准）。

1. **新建确定性转换器 `scripts/tech_doc_md2html.py`**：md → stage2 排版稿。
   复用模板的 `<style>` 与封面，自动生成目录、`<div class="chapter">` 分章、
   二级标题转中文数字（一、二、…）、三级标题转（一）（二）…，
   表格题注按**表头签名**取自脚本内嵌的 `CAPTIONS` 表。
   **连跑两次 md5 一致**（`be253a64…`，58405 B），17 张表全部有题注。
   ⚠️ 第一版把题注从"上一版 HTML"里抓，导致**脚本读自己上一轮的输出**、越跑越脏 → 改为内嵌。
2. **把 docx 有、md 缺的两张表补回 md** §5.10（待办 5 类类别）与 §5.11（5 类报告口径）。
3. **新建 `_sync_docx_58_511.py`**：把 docx §5.8–5.11 正文整段换成 md 版本
   （内联 `code`→Consolas 字体、`**粗体**`→bold run，与文档既有排版一致；
   两张表原样搬移保留表格样式）。结果：删 30 段 + 2 表，写入 4 小标题 / 17 段 / 4 项目符号 / 2 表。
   ⚠️ **翻车一次**：用 `next(p for p in d.paragraphs if p.text == "六、接口清单")` 找结束锚点，
   命中了**目录里的同名纯文本行**（在正文之前）⇒ 删除区间越过锚点吃到文档末尾，
   **一次删 115 段 + 9 表**。已从 `.presync` 备份回滚，改为**样式(Heading 2)+文本**双条件匹配，
   并加 `20 < 区间段数 < 60` 断言。
4. 两份 docx md5 一致 = `37eec052…`（73262 B）；`01-需求与技术文档/` 那份是主副本。
   docx 现有 245 段 / 17 表，整段粗体回退 0 处，附录 B 参数表 19 行。

### 第 2 项：清理 .bak

31 个 `.bak-*` 先打包到 `C:\Users\Public\uas_doc\delivery_bak_archive_20260917.zip`（581 KB），再全部删除。
交付包内 `.bak-*` 现为 0。
仍保留/待定：`01-需求与技术文档/回填前备份/`（3 个有意保留的旧版 docx）；
`02-源代码/ai-assistant/_lab/`（11.29 MB / 471 文件，含 `edge-profile-landing` 等临时残留）。

### 沉淀

`project-delivery-package` 技能新增「刷新中间产物（HTML 排版稿 / docx）」小节，含三条实测经验：
① 转换器配置内嵌、禁止读自己上一轮输出（验收=连跑两次 md5 一致）；
② docx 锚点必须「样式+文本」双条件 + 区间大小断言（附 115 段翻车案例）；
③ md 与下游冲突时用「与实测/代码是否一致」定胜负，别默认 md 赢。
并修正了该技能里「技术文档流水线刻意不做技能」的旧结论。

---

## 下午：以交付 docx 为基准的「实现完整性 / 冗余 / 缺陷」三查（新增报告）

用户给定基准：`01-需求与技术文档/AI智能助手系统_技术实现与部署文档.docx`
（17 张表；接口清单表 10×3，登记 70 接口 / 10 组）。产出
`02-源代码/ai-assistant/reports/代码实现与测试查验报告.md`。

### 查法（可复用）

1. **真实路由清单**：`from app.main import app; app.openapi()["paths"]` ⇒ **82 路径 / 91 操作**
   （组：system6 / chat3 / crm20 / student22 / org8 / operation18 / todo4 / data3 / **通知5(新组)** / internal2）。
2. **doc↔code 逐条比对**：把文档接口串解析成 `(METHOD, path)`，**路径参数先归一成 `{}`**
   （否则 `/leads/{id}` vs `/leads/{lead_id}` 会误判成「文档有代码缺」）。
   ⇒ **文档有代码缺 = 0**；**代码有文档未登记 = 21**（doc 滞后）。
   21 条分布：通知中心 5 / 研判规则版本化 6 / 学业考务 DDL 6 / 学业进度 2 / 报告分发 2。
   ⚠️ 文档写 `PATCH /alerts`，真实是 `PATCH /alerts/{alert_id}`（写法不准，非缺实现）。
3. **AST 冗余扫描**：
   - `response_model=` 全项目 **0 次** ⇒ `schemas.py` 51 类中 **24 个 `*Out` 全仓无引用**（真正死代码）。
   - 未用导入 3 处：`org.py:21 get_principal` / `agent_tools.py:34 Sequence` / `nl2sql.py:19 field`。
   - 未被调用函数 1 个：`agent_tools.py:995 cancel()`（两段式确认只做了 preview→confirm，**无 cancel 通道/无 API**）。
   - 散落物：根 `_probe.py`（手工调试脚本）、`_lab/`（11.29 MB 临时）。
   - 非缺陷：`db.py` 的 `from . import models  # noqa`（有意注册表）；`from __future__ import annotations` 35/39（3.13 下非必需）。
4. **三层实测（今日全绿）**：
   - `pytest -q --cov=app` ⇒ **415/415**，**覆盖率 88%**（5738 语句 / 675 未覆盖；10 文件 100%）。
     最低模块：`scheduler.py` 64% / `db.py` 65% / `agent_tools.py` 67% / `notify.py` 73% / `system.py` 77% / `asr.py` 80%。
   - `smoke_test.py --base http://127.0.0.1:8010` ⇒ **149/149**（45758 ms）。
   - `frontend_e2e.mjs`（Edge）⇒ **37/37**（页面异常 0 / 422 0 / 404 资源 0）。
     运行期 2 条 console 错误（403、ERR_UNSAFE_PORT）**是用例自身预期**。

### 结论

功能无缺失、无功能性缺陷；doc 接口表滞后 21 条需回填；24 死 schema + 3 未用导入 + 1 死函数 + 2 临时物待清。

### 环境备忘（本次踩到）

- 就绪探针正确路径是 **`/api/v1/ready`**（不是 `/api/v1/health/ready`，后者 404）。
- 在 `ai-assistant` 目录下 `import docx` 会报 `module 'docx' has no attribute 'seek'`
  （CWD 有同名干扰）⇒ **解析 docx 时切到 `C:\Users\Public\uas_doc` 再跑**。
- 跑 E2E：先 `serve_frontend.py --port 8020`，再
  `WB_NODE_WORKSPACE=.../binaries/node/workspace node scripts/frontend_e2e.mjs`；
  收尾用 `taskkill /PID <pid> /F /T`（PID 从 `netstat -ano | tr -d '\r'` 取）。

---

## 18:10 查验报告 5 项整改全部落地（用户「同时进行」）+ 全量回归

用户确认「5 项建议动作同时进行」，同日执行完。**先备份**：
`C:/Users/Public/uas_doc/ai_assistant_pre_cleanup_20260917.zip`（412 文件 / 6.23 MB，无 git 时唯一安全网）。

### 关键更正：死 schema 是 **20** 个不是 24

第一版 AST 扫描把「只被 `schemas.py` 内部引用」的类也算死，得出 24。改用**传递可达性**
（roots = 外部引用 → 沿内部引用做闭包）复核，真死 **20**：
`RuleItem`/`RuleProduct`（`RuleImportRequest.products` ↔ `RuleProduct.rules`）、
`DeadlineItem`/`ProgressItem`（`DeadlineImport.items`/`ProgressImport.items`）**都是活的，不能删**。
⇒ 教训：**判死代码不能只看「本文件外有无引用」，要算可达闭包。**

### 五件事

| # | 动作 | 落点 |
|---|---|---|
| 1 | 回填接口清单 21 条 + 新增「通知」分组 | `stage1/final_draft.md`、`01-需求与技术文档/*.docx`（deepcopy `<w:tr>` 插行）、`stage2/formatted-*.html`、附录 B；crm 14→20、student 14→22、operation 16→18 |
| 2 | 删死代码 | `app/schemas.py` 51→31 类 + 去失效的 `ConfigDict`；清 3 处未用导入（`org.get_principal`/`agent_tools.Sequence`/`nl2sql.field`） |
| 3 | `cancel()` **补接线**（不删） | `api/v1/tools.py` 收 `{"cancel":token}`；`agent_tools.invoke()` 加 `cancel_token` 形参；信封加 `cancelled`/`status`。写工具两段式确认 = **preview → confirm / cancel** 完整语义，**不新增路由** |
| 4 | 移出临时物 | `_probe.py` + `_lab/`(13 MB) → `C:/Users/Public/uas_doc/ai_assistant_moved_out_20260917/`（归档不直删） |
| 5 | 补单测 25 条 | 新 `tests/test_scheduler.py`(12)、`test_db_utils.py`(9)、`test_agent_tools_guards.py`(4)，`test_agent_tools.py` +4(cancel) |

### 回归（全绿）

`pytest` **440/440**（覆盖率 **90%**）· `smoke_test.py` **149/149** · `frontend_e2e.mjs` **37/37** · API 操作数不变 **91**。
覆盖提升：`scheduler.py` 64%→**100%**、`db.py` 65%→**96%**、`agent_tools.py` 67%→**78%**。
基线文档全同步 415/88% → **440/90%**：`README.md`、`reports/验收基线汇总.md`、`reports/需求完成情况查验报告.md/.html`、
技术文档 md/docx/HTML。

### 本次踩坑

- **SQLite `ALTER TABLE ... DROP COLUMN` 被索引挡住**：库里有 `ix_lead_screening_review_status`，
  必须先 `DROP INDEX IF EXISTS` 再 `DROP COLUMN`，否则报 `error in index ... after drop column`。
- **`pytest -q` 会吞掉汇总行**（`pytest.ini` 已内置 `-q`），想看「N passed」就**别再加 `-q`**。
- 报告数字口径要分三层写：**查验时快照 / 整改后实测 / 原基线**，别混成一个数。



---

## 19:50 收口 notify/system/asr 三个低覆盖模块 + 报告同步

用户确认「两件都做」：① 报告+技术文档上传资料库 ② 收口剩余低覆盖模块。

### ① 上传资料库（WorkBuddy 原生资料库，非 tdrive）

⚠️ **`mcp__netdrive` 在本会话不可用**（ToolSearch 查不到任何 netdrive 工具，
deferred tools 里只有一个裸 `mcp__netdrive`）⇒ **tdrive 传文件做不到**。
改用 `资料库` 技能（`libary/space_api.py` + `doc/create_doc.py` / `drive/upload_drive_file.py`）：

- 鉴权：`runtime_context.py` 报 `mode=client` ⇒ 必须 `ToolSearch("connect_open_platform")`
  → `DeferExecuteTool(skill_id="library")` 拿 token，再 `printf token | 脚本 --token-stdin`。
- 空间：`space.workspace.list-user-spaces` 只有 2 个 —— 个人空间 `1wE4CRdEkV6vcgFHSC9Dum`（owner，可写）
  和团队空间 `cCwTkzCwCevtZDBkVGnFEv`「🚀资料库的100种用法」（**reader，不可写**）⇒ 只能落「我的文档」。
- 结果：报告 md → 在线文档 `https://www.workbuddy.cn/space/d/ZaoabQk27Cvgj4sHcflBf3`；
  技术文档 docx → 网盘文件 `https://www.workbuddy.cn/space/d/3RhfMCGbr14nbZBhbxjdQP`。
- ⚠️ 这两个是**上传前的版本**，收口后内容已变（440/90% → 472/91%），若要刷新需用户确认后**全量覆盖**。

### ② 补 32 条测试，三模块全部拉到 100%

| 新增/追加 | 条数 | 锁住的分支 |
|---|---|---|
| `tests/test_notify_delivery.py`（新） | 15 | `_post_json` 2xx/非 2xx/抛异常；`_deliver` 四通道载荷；`send` 外发成功不降级、**外发失败兜底落站内且留原因**；标题截断/空标题；`mark_read(read=False)` |
| `tests/test_asr.py`（新） | 12 | Provider 选择（大小写不敏感 / **未知厂商显式 AppError，不静默退 mock**）；Dify 转写成功、`UpstreamError` 透传、未知异常包装；槽位抽取 |
| `tests/test_system.py`（追加） | +5 | 健康探针 **DB 不可用→degraded/down**（依赖注入假 session）；停用账号 **403 非 401**；改密全链路（太短 400 / 原密码错 401 / 生效）+ 审计留痕 |

⚠️ 改密测试用**临时账号**（`probe_pw*`，用完删），不改种子账号密码 —— 种子账号是 session 级夹具登录的，
改掉会重现「按文件字母序执行才通过」的假绿。

### 结果

`notify.py` 73%→**100%** · `system.py` 77%→**100%** · `asr.py` 80%→**100%**；
全量 **472 passed / 覆盖率 91%**（TOTAL 5576 / 516 未覆盖，15 个文件 100%）。
本轮**未改任何 `app/` 代码** ⇒ smoke/E2E 结论不受影响（未重跑，已在报告里注明）。

### 踩坑（可复用）

- **改 docx 数字前先查实际文本**：md 里写的「（TOTAL 5576 语句 / 568 未覆盖）」在 docx 里根本没有，
  docx 是「实测结果：440 条用例全部通过，整体覆盖率 90%。」；覆盖率表的模块名是 `services/audit.py`
  **不带 `app/` 前缀**，按 md 的写法替换会 0 命中。
- docx 表格加行用 `copy.deepcopy(row._tr)` + `prev_tr.addnext(new_tr)`，再用
  `[_Cell(tc, table) for tc in new_tr.tc_lst]` 包回 Cell 才能写文本；删旧行用 `row._tr.getparent().remove(row._tr)`。
- `scripts/tech_doc_md2html.py <md> <html>` 重生成 stage2 HTML，输出里的字节数是**输入 md 的大小**，不是产物大小（别误判成 HTML 缩水）。

### 20:00 资料库覆盖新版 + 「提权」可行性结论

- ✅ 用新版**全量覆盖**两份产物，**节点链接不变**：
  报告 `ZaoabQk27Cvgj4sHcflBf3`、技术文档 docx `3RhfMCGbr14nbZBhbxjdQP`。
  - 覆盖文档：`create_doc.py --node-block-id <id> --space-id <sid> --confirm-overwrite --content-file <md>`（省 `--title` 则保留原标题）。
  - 覆盖网盘文件：`drive/upload_drive_file.py <path> --node-id <blk>`（**`--node-id` 是「替换」与「新建」的唯一区别**，不传就会多出一份文件）。
  - 回读 `get_doc_reviews.py --page-id <id>` 确认新内容已生效（命中 472）。
- ❌ **「把我提成可写人员」做不到**：`space_api.py --help` 全量 API 里**没有任何改成员/角色的接口**，
  只有只读的 `space.permission.collaborators`。且团队空间「🚀资料库的100种用法」的 `createdBy` 是
  别人的 uid（`37cb9311-…`），当前用户 `myRole=reader` —— 只能由空间创建者在资料库 UI「成员管理」里改。
- 💡 可行的替代：`space.workspace.create-space --title <名>`（POST `/space/api/agent/v1/create-space`）
  **自建团队空间，角色固定为 `team/owner` ⇒ 天然可写**。按技能 `mutation.md`，建团队空间前必须先向用户确认。

### 20:06 建自属团队空间并归档交付产物（替代「提权」）

用户批准替代方案。新建团队空间 **`3VpN9myEMxGRDtuks5OxGx`「留学机构 AI 助手系统 · 交付文档」**
（`create-space` 返回 `category=team, role=owner` ⇒ **天然可写**），并把两份产物放进去：

| 产物 | 节点 | 链接 |
|---|---|---|
| 查验报告（doc） | `pmr3x19h856grfInxnWkSW` | https://www.workbuddy.cn/space/d/pmr3x19h856grfInxnWkSW |
| 技术文档（drive/docx） | `RVS1zvdVxTj4HmIVQKGnZA` | https://www.workbuddy.cn/space/d/RVS1zvdVxTj4HmIVQKGnZA |

空间入口：https://www.workbuddy.cn/space/s/3VpN9myEMxGRDtuks5OxGx
（`space.workspace.list-node --space-id <id>` 回读确认 2 个节点就位。）

⚠️ 「我的文档」里早先那份（`ZaoabQk27Cvgj4sHcflBf3` / `3RhfMCGbr14nbZBhbxjdQP`）**删不掉** ——
`mutation.md` §当前不支持 明确写了「文件/文件夹节点删除没有正式 Agent API」，只能由用户在界面上删。

### 20:06 盘点「项目还能完善什么」—— 工程侧缺口已实测核实

需求侧缺口见 `reports/需求完成情况查验报告.md` §五/§六/§十（不用重复记）。本次**新核实**的工程侧事实：

| 事项 | 核实方式 | 结论 |
|---|---|---|
| 版本控制 | `find -maxdepth 3 -name .git` | **全仓 0 个 .git** ⇒ 今天所有改动只能靠 zip 兜底（`ai_assistant_pre_cleanup_20260917.zip`），**最大工程风险** |
| CI/CD | 找 `.github/`、Jenkins/Azure 配置 | **无** ⇒ 472+149+37 三层回归目前**全靠手动起服** |
| 限流熔断 | `grep -rln "slowapi\|limiter\|ratelimit" app/` | **0 命中** |
| 字段级加密 | `grep -rln "Fernet\|AES\|encrypt" app/` | **0 命中**（`id_card_masked` 只是命名约定） |

覆盖率现状：最低是 `agent_tools.py` **78%**（26 个工具里 live-only 处理器离线不可达），其次 `material.py` 82% / `mock_agent.py` 82% / `dify.py` 85% / `db.py` 96%。

## 20:30 工具层分支收口（一项一项做 ③）

- 新增 `tests/test_agent_tools_coverage.py`（30 条）：读工具过滤分支、写工具成功路径、
  注册期自检 `_validate_registry`、`_coerce` 参数收敛、`confirm` 兜底。
- `app/services/agent_tools.py` **78% → 100%**（525 语句 0 未覆盖）；全量 **502 passed / 93%**。
- 一键回归 `scripts/regression.py`：pytest 502/93% + 起服体检/HTTP 冒烟/浏览器 E2E **23 项全过**。
  本轮只加测试，**未改 `app/` 任何代码** ⇒ 接口面（91 操作 / 82 路径）不变。
- 基线数字 472/91% → 502/93% 已同步到 README / 验收基线汇总 / 需求完成情况查验报告(md+html) /
  代码实现与测试查验报告（新增 §七）/ SKILLS.md / final_draft.md / stage2 HTML / 技术实现 docx。
- ⚠️ 发现未改：`_screening_batch` 里 `lead_ids` 的 `_lead_text()` 在 try 之外，
  单个客户不存在会炸整批，与「单条失败不影响其余」的承诺不符（改动约 5 行，待确认）。
- git：`33220c2`（13 文件）。

## 20:38 修复 _screening_batch 失败隔离（用户确认后执行）

- `app/services/agent_tools.py`：`lead_ids` 的 `_lead_text()` 改为逐条容错，客户不存在只记该条
  失败（序号与提交顺序一致），不再炸整批 —— 与「单条失败不影响其余」承诺一致。
- 测试 `test_screening_batch_guards_and_partial_failure` 同步断言 2 成功 / 2 失败 + 序号 0-3。
- 回归重跑全绿：502 passed / 93% + 体检/冒烟/E2E 23 项。待办 rmlTvC 已完成。
- 查验报告 §7.1「发现并已修复」；验收基线汇总 §7.2 同步。这是本轮唯一 `app/` 行为变更。

## 20:50 客户行级隔离（一项一项做 ④）

- `app/api/v1/crm.py`：新增 `_visible_lead()`；employee 只能看/碰自己名下客户，
  `GET /leads` 列表与 owner_id 检索对员工强制收敛为自己；manager/admin 全量。
- `test_lead_row_level_isolation`：teacher 越 7 条路径 403 + 正向 2 条。
- 回归：503 passed / 93% + 体检/冒烟/E2E 23 项；接口面不变。
- 遗留（待办 rvqUBB）：Dify 工具 lead_query 对 employee 仍查全量，等产品定口径。
- ⚠️ 行为变化：员工端客户列表从「全量」变「只看自己名下」—— 演示/答辩时注意口径。

## 21:05 限流 + 监控探针（一项一项做 ⑤）

- `services/ratelimit.py`：滑动窗口限流（auth 每 IP 30/min、chat 每账号 120/min，
  流式同桶）；429 + code=42900 + retry_after；`RATE_LIMIT_ENABLED` 可关。
- `services/metrics.py` + 中间件：`/health` 新增 metrics（请求/p95、意图命中
  dispatch/clarify/fallback、置信度、Top5 意图、限流拒绝）。
- `test_rate_limit_metrics.py` 9 条，两新模块 100% 覆盖。
- 回归：512 passed / 93% + 23 项；接口面不变。
- ⚠️ 多 worker/多机时限流与指标需换 Redis 实现。

## 21:25 字段加密 + 内容安全（一项一项做 ⑥，六项全部收口）

- `services/crypto.py`（100% 覆盖）：手机号三件套 —— `phone` 打码值 / `phone_enc` Fernet 密文 /
  `phone_hash` HMAC 等值检索。默认关闭（`FIELD_ENCRYPTION_KEY`），关闭时与旧行为逐字节一致；
  查重与工具检索 `or_(HMAC, 明文)` 兼容并存期；`scripts/encrypt_backfill.py` 幂等回填存量。
- `services/content_safety.py`（100%）：chat 入口 PII 打码后才进 Dify/审计；违禁黑名单直接拒绝
  （agent=guard / intent=content_blocked + chat.blocked 审计）。
- models：CustomerLead 加 `phone_hash`/`phone_enc` 两列（可空，启动自动补列）。
- `test_crypto_safety.py` 19 条。回归：**531 passed / 93%** + 23 项；接口面不变。
- 坑：回填跳过判定要用 `"*" in phone` 而不是 startswith（打码值以数字开头，mask(mask(x)) 会打碎号码）。
- 数字 512→531 已全量同步（md/HTML/docx，docx 覆盖率表补 crypto/content_safety 两行，
  备份 …_pre_531.docx）。查验报告 §十、验收基线汇总 §十。

## 23:00 Dify 工具 lead_query 行级收敛（第 7 项，方案 A）

- `agent_tools._lead_query`：非 manager 强制 `owner_id` 收敛；新增 `_operator_employee_id()`
  （actor → SysAccount → employee ref_id，要求在职 + 员工角色 + 有 ref_id）；
  身份不可确认 → 空结果 + `hint`（fail closed）；返回带 `scope`=all/own/unknown_operator。
- ⚠️ **部署配套**：工具通道默认 actor="dify"，员工查询会拿到空 —— Dify 调用必须带操作人登录账号名
  （`actor`），与 `_dify_inputs` 注入 role/student_id 同类要求。
- 测试：+2（行级 scope、身份解析）；`test_tool_lead_lookup` 改写新契约，静态 Key 用例带 actor。
- 回归：pytest **533/533（93%）**，agent_tools 仍 100%。
- ⚠️ 冒烟 3 条对话断言失败：**本机 Dify 掉线**（127.0.0.1/v1 超时 2.03s，.env 为 DIFY_MODE=live，
  三条耗时均 ~2050ms），非代码问题；`DIFY_MODE=mock` 重跑 **149/149**。
  ⇒ 答辩/演示前要么拉起 Dify 容器，要么把 .env 切回 mock。
- 待办 rvqUBB 已闭环；查验报告 §十一、验收基线汇总 §十一；数字 531→533 全量同步（md/HTML/docx）。

## 23:20 部署配套（第 7 项收尾）

- **实测盘点（只读）**：登录 Dify 控制台遍历 9 个应用草稿图 —— 只有「学生助手 Chatflow」调了工具，
  且是 `my_scores` / `my_requests` / `my_tickets` 三个**学生视角只读工具**；
  **没有任何节点调用 lead_query / lead_lookup** ⇒ 行级收敛对当前 live 链路**零影响**，Dify 侧无需改动。
- 后端兼容：`_operator_employee_id()` 自动剥 `dify_user_prefix`（`sys.user_id` = `uas-advisor`）。
- 新增 `scripts/check_dify_tool_actor.py`：遍历草稿图，凡调 lead_query/lead_lookup 未带 `actor` 判红（退出码 1）；
  凭据走环境变量不入库，未配置则跳过（退出码 2）。当前运行 ✅ 无违规（3 个节点）。
- 控制台 API 要点：读写都带 X-CSRF-Token、127.0.0.1 禁代理、普通 chat 应用无草稿图。
- 回归：pytest 533/533（agent_tools 仍 100%）。查验报告 §十二。

## 23:40 收尾四项（CI / 压测 / 生产守卫 / 答辩材料）

- **CI**：`.github/workflows/ci.yml`（git 根）—— pytest+前端 import → launcher --selfcheck 冒烟，
  不跑浏览器 E2E；冒烟报告 upload-artifact。
- **压测**：`scripts/load_test.py` + `reports/load_test_report.md`。mock 模式 20 并发 p95≤80ms、
  50 并发 p95≤241ms、0 服务端错误、吞吐 326–998 req/s；429 与 5xx 分开统计（多账号轮转防误判）。
- **生产守卫**：`scripts/check_prod_config.py` 9 项静态检查（默认密钥/debug/CORS/限流/加密/Dify Key），
  支持 --json/--strict。本机 3 FAIL（SECRET_KEY、TOOL_SHARED_SECRET、DEBUG）+ 2 WARN（SQLite、未开加密）。
- **答辩材料**：`docs/答辩-成绩管理模块.md` —— 模块边界/角色场景/接口表（student_scores vs my_scores 分工）/
  数据模型（Numeric 定点数理由）/分层与关键代码/测试对照/3 分钟演示脚本/10 条 Q&A。
  注意：本项目**没有独立 crud 层**（API 直连 Session，复杂业务下沉 services），答辩别讲成四层。
- 开发库已备份 `C:/Users/Public/uas_doc/devdb_backup_20260917_2316.db`。

## 23:40 配置加固 + 演示前状态确认

- `.env`（gitignore，不入库）按守卫脚本整改：SECRET_KEY / TOOL_SHARED_SECRET 重新生成为 64 字符随机值，
  `DEBUG=true` → `false`；`check_prod_config.py` 现在 **0 FAIL**（剩 WARN：SQLite、未开字段加密）。
  备份 `C:/Users/Public/uas_doc/env_backup_20260917_2334.env`。
- ⚠️ 换密钥 = 旧 JWT 立即失效（无状态），需重新登录；交付包 .env 模板仍是默认值，靠守卫脚本拦。
- Dify 探活 **7/7 通过**，保持 `DIFY_MODE=live`；回归 533/533 + 23 项（live 冒烟 149/149）。
  演示当天先验 `python scripts/check_dify.py`，掉了就改 `.env` 的 DIFY_MODE=mock 重启。
- CORS_ORIGINS 已含 8020/5173，关 DEBUG 后前端正常。

## 23:43–23:49 版本验收（阶段五：测试验收）

- 实跑全量回归：`scripts/regression.py` → **pytest 533/533（93%）16.2s** + **体检/冒烟/E2E 23 项 82.4s**；
  冒烟 149/149（live，28.1s）、E2E 37/37（json 23:46:48 刷新）、Dify 探活 7/7、配置守卫 0 FAIL/2 WARN。
- 产出 **`reports/版本验收报告.md` + `.html`**（HTML 用 `_md2html_audit.py`，需先 cp 一份同名 .html 作样式模板，脚本只读它的 `<style>`）。
- 结论：**交付/答辩验收 ✅ 通过**；**生产上线 🟡 有条件通过**（R1 甲方规则文件 / R2 26 工具 Dify 侧联调 /
  R3 真实触达通道 / R4 准确率样本集 / R5 渗透测试 未达成）。
- 观察项 O1：`frontend_e2e_report.md` 停在 09-15 20:36，同轮 json 已刷新 → md 与 json 不同步，易被误读为没重跑。
- O2：live 冒烟 28.1s vs mock 563ms，演示时两个数必须分开讲。

### 遗留问题已建为项目事项（16 条，全部指派又肥了三金）

P0 urgent：R1 甲方规则文件 `rPQBSZ` / R2 工具联调 `rEAHCI` / R3 触达通道 `rm60Au` /
R4 样本集 `rMdBlp` / R5 渗透 `rVlrX9`
P1 high：R6 HTTPS `rNpnhV` / R7 字段加密 `rWe7Ph` / R8 MySQL `r8T7NJ` / R9 Redis `raX6ht` / R10 live 压测 `rJPr2b`
P2：R11 教务 `ri4YxJ` / R12 CRM `rwkkTW` / R13 知识库 `rSofNt` / R14 ASR `r2XuQm` / R15 环境 `rf5nsj`(low)
观察项：O1 E2E md/json 不同步 `rnXldA`(low)
事项 ID 已回写进 `reports/版本验收报告.md` §九，报告与事项双向可追溯。

## 验收结论已上传资料库（团队空间「留学机构 AI 助手系统 · 交付文档」`3VpN9myEMxGRDtuks5OxGx`）

- 在线文档「版本验收报告（ai-assistant）」：https://www.workbuddy.cn/space/d/9AWWOLOq8kwsFgslCAZ0DU
- HTML 版（web 节点，可直接打开/打印）：https://www.workbuddy.cn/space/d/0cLz6JYKjMJcEjkxBbEuJb
- 原有 2 个节点（查验报告 `pmr3x19h856grfInxnWkSW`、技术文档 `RVS1zvdVxTj4HmIVQKGnZA`）未动。
- 上传前把正文里的本机绝对路径全部脱敏成 `ai-assistant/`（资料库产物不写本地路径）。
- ⚠️ 坑：**HTML 不能走网盘上传**（`drive/upload_drive_file.py` 报「该文件类型不走网盘上传」），
  要改走 `page/import_html.py --space-id <spaceId> --file-name <中文名>.html`；导入前须过图片外链硬门
  （无 `<img>` 第三方外链、无 base64 内联），本报告的 HTML 天然合格。
