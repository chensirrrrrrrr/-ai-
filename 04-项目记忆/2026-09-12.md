# 2026-09-12

## 客户需求表 → 需求文档 + 技术文档

- 输入：`C:\Users\机械革命\Desktop\客户需求表.xlsx`
  - sheet `客户需求`（A1:C6）：需求项 / 详细描述 / 需求详细拆解，5 大模块 —— 客户研判、客服Agent、企业智能助手、学生智能助手、智能报告。
  - sheet `开发计划`（A1:F3）：ID / 开发内容 / 计划完成时间 / 优先级 / 前置依赖 / 负责人，仅 2 条（意向客户表接口、企业智能助手意向客户意图）。
  - 项目本质：留学机构的 AI 智能助手系统（客户-员工-学生-管理层四类角色）。
- 产出（写在桌面，源 md 留在 `C:\Users\Public\wb_doc\`）：
  - `AI智能助手系统_需求规格说明书.docx`（9 章 + 待确认事项，607 段 / 17 表）
  - `AI智能助手系统_技术方案设计文档.docx`（13 章，1017 段 / 29 表）
  - 生成通道：editor_sdk（`create_doc` → `doc_insert_markdown` 读 `file://C:/...md` → `doc_modify_section` 设 A4 → `doc_set_page_number` → `save_file`）。
- 主动补充的内容（原表没有）：项目背景与目标、用户角色、非功能需求、验收标准、待确认事项 12 项、建议 WBS 排期、技术选型、数据模型 17 张表、接口清单 24 条、5 个关键流程、风险与应对 10 条。

## 新增 / 更新的 skill

- `~/.workbuddy/skills/editor-sdk-docx`：补了 4.5~4.8 节
  - `doc_insert_markdown` 的 `markdown` 字段可直接写 `file://C:/path/x.md` 读文件（三个斜杠 `file:///C:/` 会报 cannot open）。
  - ⚠️ markdown 围栏代码块会被渲染成固定 16×16cm 内联文本框，正式文档里改用表格。
  - `create_doc` 出的文档没有 sectPr，需 `doc_modify_section` 补 A4 + 页边距、`doc_set_page_number` 补页码。

## 给技术文档第 6 章插入 ER 图（17 张表）

- 产出 5 张浅色 ER 图（mermaid → Edge 无头 2x 截图 → PIL 裁白边），插在「6.1 数据表总览表格之后、6.2 标题之前」：
  - 图6-1 客户与组织域（员工/意向客户/客户跟进记录/客户研判结果/员工日报/学生）
  - 图6-2 学生行政服务与售后 / 图6-3 学生学业与心理关怀 / 图6-4 活动与活动报名 / 图6-5 系统支撑与统计
  - 尺寸 14.50×8.07 / 13.73×8.92 / 14.50×6.27 / 14.50×9.02 / 11.48×8.92 cm，全部 ≤ 14.66cm 正文宽。
- 图片素材与脚本都在 `C:\Users\Public\wb_doc\`（fig61~fig65v2.png、do_insert2.py、verify_insert.py）。
- 备份：`C:\Users\Public\wb_doc\backup_tech.docx`（插图前版本）。
- 校验通过：5 张图各占一段、图注独立成段、`numPr` 仍 106 / `outlineLvl` 仍 52（未伤列表）、A4 未变。

## 关键结论（已写进 editor-sdk-docx skill）

- `doc_insert_image` 是**位置插入**，落在目标段落内部。直接插到标题/图注段的 begin 会与那段正文同行 →
  正确姿势是 markdown 建「占位段 + 图注段」骨架 → `doc_find` 取占位符 → **倒序**插图 → `doc_replace_text` 清占位符。
- markdown 的 ER 图按 A4 页宽插入时，等效字号 ≈ `17 × 548 / 图宽`。**单图实体数必须 ≤6**，17 张表一张图会缩到 4.7px 完全看不清。
- mermaid ESM import 在 Edge 无头下渲染不出来，必须用 UMD + `--virtual-time-budget`。

## 17 张表完整 ER 总览 → 横排页大图

- 需求：第 6 章 6.1 之后加一个**横排页**，17 张表的完整 ER 大图铺满一页；原 5 张分域图保留不动。
- 图：`C:\Users\Public\wb_doc\er_overview.png`（3905×916；mermaid `fs=48 / minEntityWidth=318` →
  插到 995px 宽时等效字号 12.4px、高 6.17cm），源 `_rels.txt`。
  - 17 实体图的等效字号是**封顶**的（≈13px）：布局宽度主要随盒子宽度涨，比字号快。
    实测 24→10.6px、36→11.1、40→11.6、48→**12.4**、144→虚高（量错）。
  - ⚠️ 量尺寸**别用** `msedge --screenshot`：`--window-size` 会裁掉图，所有字号都返回 6969px 假数据；
    改用 puppeteer-core 量 `svg.getBoundingClientRect()`（脚本已存进 skill `scripts/measure_er.js`）。
  - ⚠️ mermaid 中「带属性块的实体」其实体名约 3 个汉字就折行，且与 `minEntityWidth` 无关
    → 孤立表（课程项目 / 知识库文档）必然折行，只能接受。
- 文档改法（结果已验证）：
  1. `do_landscape.py`：在「6.2 核心表结构」标题前 `doc_insert_markdown` 插「说明段 + @IMG@ + 图注」
     → 占位插入法插 `er_overview.png` → `doc_replace_text` 清占位符 → 保存。
  2. `landscape_sect.py`：手工改 `word/document.xml` 塞 2 个 `w:sectPr`——说明段塞 portrait（节1 收尾）、
     图注段塞 landscape（节2 = 横排页）→ 共 3 节。
     - 同时删掉 body 末尾 sectPr 的 `pgNumType start="1"`（否则第 3 节页码从 1 重开）。
     - 同时把图的 `wp:extent`/`a:ext` 由 5276850×1235684 改成 9477938×2223250 EMU
       （`doc_insert_image` 在竖排状态下会被夹到正文宽 554px = 14.66cm）。
- **重大发现**：`doc_insert_section_break` / `doc_get_section_property` **本来就存在**（doc.md 里有）。
  之前"editor_sdk 没有分节符工具"的判断是错的 —— 以后做横排页优先用官方工具，别手工改 XML。
- 校验（WPS 引擎自己读回 `doc_get_section_property`）：`section_count=3`；
  节0 portrait 595.3×841.9pt（range 0-5567）；**节1 landscape 841.9×595.3pt**（range 5567-5592，
  页边距 42.5/56.7pt = 1.5/2cm）；节2 portrait（5592-13559）。
  `w:drawing` 6 张、`numPr` 106、`outlineLvl` 52 均未变；内嵌 `word/media/image6.png` 与 er_overview.png 字节一致。
- 备份：`backup_tech2.docx`（插图前）、`backup_tech3_pre_sect.docx`（加分节符前）。
- 踩坑：手工字符串替换必须**按 offset 从大到小**做，否则前面的改动会让后面的 offset 失配 →
  XML 被切坏（`not well-formed (invalid token)`，报错列 89292 正好是图段落起点）。
- skill `editor-sdk-docx` 已补 4.10.1（等效字号封顶 + 精确测量）与 4.11（横向节两种做法）+ 自检项。
