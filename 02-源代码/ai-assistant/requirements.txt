# 运行环境：Python >= 3.11
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
sqlalchemy>=2.0,<3.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3
httpx>=0.27
PyJWT>=2.8
python-multipart>=0.0.9

# 材料解析（M1 客户研判，REQ-M1-01/02）—— 可选增强依赖：
# 装了就用它们做高保真解析；**没装也能跑**，services/material.py 会自动回落到
# 纯标准库实现（PDF 解 FlateDecode 内容流、XLSX 解 zip 内的 XML），
# 两条路产出口径一致，响应里的 parser 字段会标明实际用了哪条。
pypdf>=4.2
openpyxl>=3.1

# 测试
pytest>=8.0
pytest-cov>=5.0
