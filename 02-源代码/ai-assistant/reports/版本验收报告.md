# 版本验收报告 · 留学机构 AI 智能助手系统（ai-assistant）

- **验收对象**：`ai-assistant`（留学机构 AI 智能助手系统）
- **项目根**：`C:\Users\机械革命\Desktop\留学机构AI助手系统_交付\02-源代码\ai-assistant\`
- **验收时间**：2026-09-17 23:43 – 23:47
- **验收方式**：全量回归实测（pytest + 覆盖率 → 起服体检 → HTTP 冒烟 → 浏览器 E2E）+ 外部依赖探活 + 生产配置静态守卫 + 历史查验报告复核
- **验收依据**：SRS V1.0（需求规格）、TDD V1.0（技术方案）、客户需求表、`reports/需求完成情况查验报告.md`、`reports/代码实现与测试查验报告.md`、`reports/验收基线汇总.md`

---

## 一、验收结论

| 维度 | 结论 |
| --- | --- |
| **交付验收（演示 / 答辩 / 结题）** | ✅ **通过** —— 功能面完整、工程化证据充分、全部结果可一键复现 |
| **生产上线（PROD 准出）** | 🟡 **有条件通过（当前不建议直接上线）** —— 5 项准出门槛未达成，均属外部依赖与生产化非功能能力 |

**结论依据**：34 条功能需求判定为 ✅22 / 🟡5 / ⚪7 / 🔴0，无完全未实现项；四条自动化基线（单元 / 冒烟 / E2E / Dify 接线）本轮**全绿且为当天最新实测**；SRS 第七章 9 条验收标准中 1 条完全达成、4 条部分达成、4 条因缺少甲方样本集或真实通道**不具备验证条件**。

> 判定口径说明：🟡「有条件通过」不是质量问题，而是**验证条件缺失**——准确率类指标（AC-01/02）无甲方标注样本集、触达类指标（AC-07）无真实通道，
> 这两类只能在甲方提供材料与账号后判定，代码侧无法自证。

---

## 二、本次实测结果（2026-09-17 23:43–23:47 实跑）

| 验证层 | 结果 | 耗时 | 证据文件 |
| --- | --- | --- | --- |
| 单元 / 集成（pytest） | ✅ **533 / 533 通过**，覆盖率 **93%** | 16.2s | 命令行输出 |
| 起服体检 + HTTP 冒烟 + 浏览器 E2E | ✅ **23 项分组全过**（82.4s） | 82.4s | `reports/regression_report.md` |
| ├ HTTP 冒烟（真实起服，live 模式） | ✅ **149 / 149**，累计 28.1s | — | `reports/smoke_report.md`（23:45:34 刷新） |
| └ 浏览器 E2E（Edge 真实驱动） | ✅ **37 / 37**，页面异常 0 | — | `reports/frontend_e2e_report.json`（23:46:48 刷新）+ `frontend_shots/` |
| Dify 接线探活 | ✅ **7 / 7 [OK]**（mode=live） | — | `python scripts/check_dify.py` |
| 生产配置守卫（静态） | ✅ **0 FAIL**，2 WARN，7 PASS | — | `python scripts/check_prod_config.py` |
| 并发压测（mock，23:28 已跑） | ✅ 20 并发 p95 27.1 ms / 0 个 5xx | — | `reports/load_test_report.md` |

**一键复现**：

```bash
python scripts/regression.py          # 全量：pytest → 体检 → 冒烟 → E2E，约 100s
python scripts/check_dify.py          # Dify 7 个应用探活
python scripts/check_prod_config.py   # 生产配置守卫
```

**口径一致性复核**：接口面 82 路径 / 91 操作 / 10 组、数据表 19 张、Agent 6 个，与 TDD 登记一致；
README、验收基线汇总、两份查验报告的基线数字互相一致（533 / 93% / 149 / 37）。

---

## 三、需求完成度（34 条，M1–M5）

| 判定 | 条数 | 含义 |
| --- | --- | --- |
| ✅ 已实现 | 22 | 有代码、有接口、本次可复验 |
| 🟡 部分实现 | 5 | 主链路在，关键环节缺失或口径收窄 |
| ⚪ 依赖外部未接线 | 7 | 结构就位，真实能力挂在 Dify 知识库 / ASR / 教务 CRM |
| 🔴 未实现 | 0 | 首次查验的 3 条🔴已全部闭环 |

**🟡 部分实现的 5 条**（缺口均在代码外或数据源侧）：

| 编号 | 需求 | 缺哪一块 |
| --- | --- | --- |
| REQ-M1-04 | 双产品画像研判 | 规则引擎已落地，但判定阈值用的是内置样板规则，甲方正式规则文件未提供 |
| REQ-M2-04 | 课程与项目推荐 | 表与接口在，自动匹配规则未接进对话链路 |
| REQ-M3-01 | 语音录入意向客户 | 文字路径 ✅；语音只到「转写 + 槽位展示」，未与录入表单打通 |
| REQ-M4-03 | 售后反馈闭环通知 | 工单与 AI 摘要 ✅；「处理完毕自动通知学生」依赖真实触达通道 |
| REQ-M4-05 | 进度追踪 | 用 `student.stage` 单字段模拟，未对接院校申请 / 签证系统 |

---

## 四、验收标准达成情况（SRS 第七章）

| 编号 | 验收项 | 判定 | 卡点 |
| --- | --- | --- | --- |
| AC-01 | 研判准确率 ≥90% 且可追溯 | 🔴 | 无甲方样本集；规则为内置样板 |
| AC-02 | 7 类意图准确率 ≥90%、问答抽样合格率 ≥95% | 🔴 | 无标注集，无法实测 |
| AC-03 | 活动报名闭环，后台可查完整记录 | ✅ | 冒烟已覆盖 |
| AC-04 | 8 项功能可用；NL2SQL 正确率 ≥90% | 🟡 | 功能 ✅；无「约定问题集」正确率实测 |
| AC-05 | 指令式操作正确执行并留痕，误操作率 <1% | 🟡 | 留痕 ✅、两阶段确认 ✅；误操作率无实测 |
| AC-06 | 7 类场景可用；请假与投诉端到端跑通 | 🟡 | 请假 / 投诉 ✅；学业考务与进度追踪受数据源限制 |
| AC-07 | 高危样本触发、触达时延 ≤5s、无漏报 | 🔴 | 无真实触达通道，时延不可测 |
| AC-08 | 5 类报告自动生成 + 四格式导出 + 定时推送 | 🟡 | 生成 ✅、导出 ✅、推送链路 ✅（分发通道为站内 / 外部待接） |
| AC-09 | 性能达标 + 渗透测试无高危漏洞 | 🔴 | 未做渗透测试；性能仅在 mock 模式压过 |

---

## 五、已闭环的加固项（本轮之前完成，本轮回归确认仍有效）

| 加固项 | 状态 |
| --- | --- |
| CRM 客户数据行级隔离（employee 只看自己名下） | ✅ 7 条越权路径 403 |
| 接口限流（登录 30/min·IP，对话 120/min·账号） | ✅ 429 + `code=42900` |
| `/health` 监控探针（QPS / 错误率 / p95 / 意图分布） | ✅ |
| 客户手机号字段级加密（三件套，默认关闭） | ✅ 代码就位，⚠️ 未开启 |
| 对话内容安全（PII 打码 + 违禁拦截） | ✅ |
| Dify 工具 `lead_query` 行级收敛（fail closed） | ✅ |
| CI 流水线 / 并发压测 / 生产配置守卫 / 答辩材料 | ✅ |
| 本机密钥轮换 + `DEBUG=false` | ✅ 守卫脚本 0 FAIL |

---

## 六、上线风险清单（未闭环项）

### P0 · 准出阻塞（不满足则 AC-01/02/05/07 无法验收）

| # | 风险 | 影响 | 建议动作 |
| --- | --- | --- | --- |
| R1 | 甲方《用户画像研判规则》及双产品判定阈值未提供，现用内置样板规则 | 研判结论与甲方口径可能不一致；AC-01 不可验收 | 向甲方索取规则文件，落进 `services/rules.py` 版本化导入 |
| R2 | 26 个业务工具在 Dify 侧仅 3 个只读节点接线，写类工具未联调 | 「指令式业务处理」在 live 模式下走不通；AC-05 | 与 Dify 侧联调并验收写工具的两阶段确认链路 |
| R3 | 无真实触达通道（企微 / 短信 / 公众号） | AC-07 永远无法验收，高危预警无法真正送达 | 确定渠道与账号，接入 `services/notify.py` 渠道适配层 |
| R4 | 准确率类指标（AC-01/02）无标注样本集 | 无法给出 ≥90% 的实测证据 | 请甲方提供历史咨询记录与判定样本 |
| R5 | 未做渗透测试（AC-09） | 上线前安全合规无据 | 上线前安排第三方或内部渗透，至少覆盖越权与注入 |

### P1 · 面向生产可用

| # | 风险 | 影响 | 建议动作 |
| --- | --- | --- | --- |
| R6 | 全链路无 HTTPS（无 TLS 终止） | 传输层明文 | 网关 / Nginx 终止 TLS |
| R7 | 字段加密未开启（`FIELD_ENCRYPTION_KEY` 未配置） | 客户手机号明文落库，拖库即泄露 | 配置 key → 备份库 → 跑 `scripts/encrypt_backfill.py` 回填 |
| R8 | 数据库为 SQLite | 单进程，无并发写保护 | 多用户 / 多 worker 部署换 MySQL（`requirements-mysql.txt` 已备） |
| R9 | 限流与指标为进程内内存实现 | 多 worker / 多机部署时失效 | 换 Redis 实现（接口不变） |
| R10 | 真实 Dify 链路未压测（现压测为 mock） | SRS 6.1「并发 ≥200 / 可用性 ≥99.5%」无证据 | live 模式下补一轮压测，与本机毫秒级数据分开报 |

### P2 · 完善度

| # | 风险 | 建议动作 |
| --- | --- | --- |
| R11 | 学业考务数据源（REQ-M4-04）依赖教务系统 | 取接口文档与测试账号后对接 |
| R12 | 进度追踪（REQ-M4-05）依赖 CRM / 申请系统 | 同上 |
| R13 | 知识库内容源为自建样本文档（26 篇） | 替换成甲方《公司信息》《公司业务》《留学政策》 |
| R14 | ASR 仍为 mock provider | 确定 ASR 供应商与方言需求后接线 |
| R15 | 无 K8s / 多环境（DEV/TEST/UAT/PROD） | TDD 列二期，按部署形态决定 |

---

## 七、本次验收发现的新问题（观察项，非阻塞）

| # | 现象 | 严重度 | 建议 |
| --- | --- | --- | --- |
| O1 | `reports/frontend_e2e_report.md` 停留在 2026-09-15 20:36，而同轮 `frontend_e2e_report.json` 已刷新为 23:46 | 低（证据新鲜度） | 让 E2E 脚本同时刷新 md，或在 md 顶部标注「对应 json 时间戳」，避免误读为未重跑 |
| O2 | 冒烟累计耗时 28.1s（live 模式）较 mock 模式（563 ms）高两个量级 | 提示（非缺陷） | 属真实模型链路固有耗时，评审时应分开陈述，不要与 mock 数据混报 |

---

## 八、演示 / 答辩前必做（Checklist）

1. `python scripts/check_dify.py` —— 先探活；若容器掉了（今晚曾发生），把 `.env` 的 `DIFY_MODE=live` 改成 `mock` 并重启即可继续演示。
2. 重新登录一次 —— 密钥已轮换，**旧 Token 立即失效**（JWT 无状态），不是故障。
3. 一键复现证据用 `python scripts/regression.py`（约 100s），不要后台挂起 launcher（stdout 管道不收口会假死）。
4. 演示口径：本系统处理耗时是毫秒级（压测 p95 27 ms），真实模型耗时是秒级，两个数分开讲。

---

## 九、验收签署建议

- **交付 / 结题验收**：建议**通过**，附本报告的实测基线与四条一键复现命令作为证据链。
- **生产上线**：建议**有条件通过** —— 关闭 R1 / R2 / R3 / R5 后再走上线评审；R6–R10 应在上线方案中明确责任人与时间点。
- **后续动作**：本验收结论上传项目资料库归档。

### 遗留问题事项清单（2026-09-17 已建，指派：又肥了三金）

| 编号 | 事项 ID | 优先级 |
| --- | --- | --- |
| R1 甲方研判规则文件 | `rPQBSZ` | urgent |
| R2 26 工具 Dify 侧联调 | `rEAHCI` | urgent |
| R3 真实触达通道 | `rm60Au` | urgent |
| R4 标注样本集与准确率实测 | `rMdBlp` | urgent |
| R5 渗透测试 | `rVlrX9` | urgent |
| R6 HTTPS / TLS 终止 | `rNpnhV` | high |
| R7 开启字段加密并回填 | `rWe7Ph` | high |
| R8 生产库换 MySQL | `r8T7NJ` | high |
| R9 限流与指标换 Redis | `raX6ht` | high |
| R10 live 模式并发压测 | `rJPr2b` | high |
| R11 教务系统数据源 | `ri4YxJ` | medium |
| R12 CRM / 申请系统对接 | `rwkkTW` | medium |
| R13 知识库换甲方正式文档 | `rSofNt` | medium |
| R14 ASR 真实 provider | `r2XuQm` | medium |
| R15 环境划分与 K8s 决策 | `rf5nsj` | low |
| O1 E2E 报告 md/json 不同步 | `rnXldA` | low |
