# 项目需求完成情况查验报告

- **查验对象**：`ai-assistant`（留学机构 AI 智能助手系统）
- **项目根**：`C:\Users\机械革命\Desktop\2026-09-12-11-04-16\ai-assistant\`
- **查验依据**：`客户需求表.xlsx`（客户需求页 / 开发计划页）、`AI智能助手系统_需求规格说明书.docx`（SRS V1.0）、`AI智能助手系统_技术方案设计文档.docx`（TDD V1.0）
- **查验方式**：静态比对（源码 / 模型 / 路由 / 配置 / 部署件）+ 实测（pytest、一键自检、冒烟、E2E）
- **查验时间**：2026-09-15

---

## 修复进展（同日 20:2x 复核）

> 本文档下方主体为**首次查验（19:49）**的原始记录。查验后已按结论推进修复，下列空档**已闭环**，
> 阅读下方正文时请以本表为准。

| 原空档 | 状态 | 修复落点 |
| --- | --- | --- |
| 🔴 REQ-M1-03 画像研判规则导入与版本化 | ✅ 已修复 | 新增 `app/services/rules.py`（规则文档版本化 / 导入 / 归档 / 激活 + `evaluate_product`），替换 Dify 硬编码占位；`scripts/seed.py` 内置样版规则 |
| 🔴 REQ-M4-04 学业考务（DDL / 考试时间 + 考前提醒） | ✅ 已修复 | 新增 `app/services/deadline.py`（DDL 导入 upsert、紧急度分桶、提醒、进度看板），`scheduler.tick()` 已挂 DDL 提醒任务 |
| 🔴 报告定时推送（SRS 4.5.3 / AC-08） | ✅ 已修复 | `reports.run_scheduled_with_push()` 打通生成→分发；`app/services/notify.py` 统一渠道 |
| ⚪ 通知渠道缺失 | ✅ 已修复 | `notify.py` 统一渠道 + 渠道降级**诚实上报**（请求 `channel` vs 实际 `delivered_via`）；闭环通知接入请假审批 / 工单结单 / 待办提醒 |
| TDD 4.5 工具表 19 个工具，实际仅 4 个只读 | ✅ 已修复 | 新增 `app/services/agent_tools.py`：19 个工具（5 只读 + 14 写）+ 4 个遗留只读；**写操作两阶段确认**（preview → token → confirm） |
| 老库缺新表/新列（`create_all` 不补列） | ✅ 已修复 | 新增 `scripts/sync_schema.py`（只 `CREATE TABLE` / `ADD COLUMN`，永不删列改类型），已接入 `launcher.ensure_database()` |

**复核实测（20:2x，同一套用例重跑）**：pytest **377/377** · 一键自检 **13/13** · 冒烟 **149/149** · 浏览器 E2E **37/37**。

**仍未闭环**（属生产化非功能能力，非本次修复范围）：HTTPS、字段级加密（`customer_lead.phone` 明文）、限流熔断、
内容安全过滤、监控看板、撤销机制、渠道接入（企微 / 公众号 / 小程序）、CI/CD、Redis 会话记忆、
向量库 / RAG / Rerank、Prompt 版本化；`GET /leads` 仍无行级过滤（角色级权限）。

---

## 结论速览

**功能需求 34 条（M1–M5）**：

| 判定 | 条数 | 含义 |
| --- | --- | --- |
| ✅ 已实现 | 19 | 有代码、有接口、可复验 |
| 🟡 部分实现 | 5 | 主链路在，关键环节缺失或口径收窄 |
| ⚪ 依赖外部未接线 | 7 | 代码结构就位，但真实能力挂在 Dify 知识库 / ASR / 教务 CRM 上，当前运行在 mock |
| 🔴 未实现 | 3 | 无对应实现 |

**非功能与验收标准**：6.1 性能 / 6.2 安全 / 6.3 可维护性 / 6.4 兼容性 / 6.5 易用性 中，
**HTTPS、字段级加密、内容安全过滤、限流熔断、监控看板、撤销机制、渠道接入、CI/CD 均未实现**；
验收标准 AC-01/02/07/09 因缺甲方样本集与真实触达通道**无法验证**。

**一句话结论**：业务功能面（34 条需求里 24 条已落地/接近落地）完成度高、工程化程度好；
**真正的缺口集中在两类**——① 3 条功能需求完全没做（研判规则版本化、学业考务、报告定时推送）；
② 面向生产的非功能能力（安全、性能、可观测、渠道、真实模型/知识库接线）基本还是空档。

---

## 一、完全未实现的 3 条功能需求（🔴）

| 编号 | 需求 | 依据 | 现状与证据 |
| --- | --- | --- | --- |
| REQ-M1-03 | **规则装载**：支持导入并版本化管理《用户画像研判规则》，规则调整无需发版 | SRS 4.1.3 | 全库无规则导入接口、无规则版本表、无规则文件路径。`app/services/material.py` 里的 `_DEGREE_RULES` / `_STAGE_RULES` / `_LANGUAGE_RULES` 只是**字段抽取**正则，不是「画像判定规则」；真正的双产品判定被丢给 Dify `screener` workflow，规则既不在本系统、也无法版本化。SRS 4.1.5「研判必须以甲方提供的规则文件为唯一依据」因此无法成立 |
| REQ-M4-04 | **学业考务**：对接教务数据，查询论文 DDL / 考试时间，并支持考前或截止前智能提醒 | SRS 4.4.2 | 19 张表里没有 DDL/考试时间数据源（`student_request` 仅支持 `type=EXAM` 的申请单，不存时间点）；无对应查询接口；`services/scheduler.py` 只挂了「待办推送」和「定时报告」两个任务，**没有 DDL 提醒任务** |
| — （AC-08 / SRS 4.5.3） | **报告定时推送** | SRS 4.5.3「支持在线查阅、导出与**定时推送**」；AC-08 | 报告能定时**生成**（`reports.due_types()` + `run_scheduled()` + `POST /reports/scheduled/run`），但生成后只写 `report_record`，**没有任何推送分发**（无 `pushed_at` / 订阅关系 / 渠道发送器）。`grep -rn "deliver\|pushed\|channel" app/services/reports.py` 只命中报告正文里的「未触达预警」措辞 |

---

## 二、部分实现的 5 条（🟡）

| 编号 | 需求 | 缺哪一块 |
| --- | --- | --- |
| REQ-M1-04 | 双产品画像研判 | 数据结构支持多产品（`lead_screening.hit_products` 为 JSON 数组），但判定逻辑在 Dify 侧；mock 实现是占位（`app/services/dify.py:578`：`{"name": f"{h}硕士直申", "match": 0.8}`，`hit` 直接按国家名硬匹配、`match` 写死 0.8） |
| REQ-M2-04 | 课程与项目推荐（按学历背景 + 意向国家自动匹配） | `course_project` 表与 `/kb/documents` 在，但「按画像条件自动匹配并推荐」的判定规则不在代码内，对话里也未接该表 |
| REQ-M3-01 | 意向客户录入（语音或文字，自动解析结构化入库） | **文字路径 ✅**（`POST /leads`）；**语音路径只到「转写 + 槽位展示」**——`frontend/js/views/chat.js` 仅在 `purpose=chat` 时把转写文本回填到对话框，未与客户录入表单/接口打通，SRS 4.3.4 要求的「识别结果人工校对与二次提交」也没有对应步骤 |
| REQ-M4-03 | 售后反馈（工单 + 处理完毕后自动通知学生「您的 xxx 投诉已解决」） | 工单创建/状态更新/满意度 ✅，AI 摘要 ✅；**「处理完毕自动回写通知学生」未实现**（无通知记录、无发送动作） |
| REQ-M4-05 | 进度追踪（打通内部 CRM / 申请系统） | 用 `student.stage` 单字段 + 5 段 timeline 模拟（`app/api/v1/student.py:67`），**没有系统对接**：文书审核 / 院校申请 / 签证办理的细分进度、时间点、责任人都不存在。TDD 7.4 列的四类外部对接（教务、CRM、HR、企业微信）**一个都没接** |
| REQ-M4-07 | 增值转化（识别升学意向 → 推送学历提升项目） | `course_project` 表在；但「识别升学意向 + 适时推送 + 频次控制 + **退订/拒绝入口**」（SRS 4.4.4）均无实现 |

> 上表 6 行中 REQ-M4-04 已计入「完全未实现」，此处不重复。

---

## 三、已实现但当前运行在 mock / 未接线（⚪）的 7 条

**共同点**：代码抽象层（Provider、App Key 分组、数据结构、降级开关）都做好了，缺的是外部依赖本身。
`.env` 当前为 `DIFY_MODE=mock`（注释写明「本机还没有可用的 Dify 实例」）、`ASR_PROVIDER=mock`。

| 编号 | 需求 | 挂在哪 |
| --- | --- | --- |
| REQ-M2-01 / 02 / 03 / 06 / 07 | 公司信息咨询、公司业务查询、留学政策查询、FAQ 自助解答、日常闲聊 | 全部依赖 Dify 知识库 + Prompt。当前回答来自 `app/services/dify.py` 硬编码的 `_MOCK_KB` 关键词匹配表；**系统内没有向量库、没有 embedding、没有 rerank**（`app/services/ops.py:407` 注释：「知识库元数据（真实切片在 Dify 数据集）」，`knowledge_doc` 表只存元数据） |
| REQ-M1-04、REQ-M3-04 | 双产品判定、口述日报的语音识别 | 同上；ASR 只有 `mock` provider（`dify` provider 代码就位但未接） |

**⚠️ 由此带来的 3 个连带缺口（SRS/TDD 要求，代码内无实现）**：

1. **M2 业务规则**「涉及价格、退费、承诺性话术的回答必须严格来自知识库，不得由模型自由生成」——无强制约束代码，也无内容安全过滤兜底。
2. **TDD 5.3 检索策略**（向量召回 Top20 + BM25 Top20 → Rerank → Top5 → 低于阈值触发拒答）与 **5.4 幻觉抑制**（温度 0.1~0.3、价格退费走 FAQ 精确匹配优先、输出前数字回溯证据）——系统内**均无实现**，全部外包给 Dify；TDD 4.2「confidence < 0.5 → 兜底引导或转人工」的阈值分支也未在 `intent.py` 中按 0.75/0.5 分档实现。
3. **M2 业务规则**「政策类信息需标注数据更新时间与来源，超过时效阈值应提示『以官方最新公布为准』」——无时效阈值判断逻辑。

---

## 四、逐条查验总表（功能需求）

判定口径：✅ 有代码有接口可复验 ｜ 🟡 主链路在但关键环节缺失 ｜ ⚪ 结构就位但依赖外部未接线 ｜ 🔴 无实现

### M1 客户研判（SRS 4.1）

| 编号 | 需求 | 判定 | 证据 |
| --- | --- | --- | --- |
| REQ-M1-01 | 多格式信息接入 | ✅ | `material.py` 支持 pdf / xlsx / xlsm / csv / txt / md；解析两路并存（pypdf·openpyxl 优先，缺库回落纯标准库），响应 `parser` 字段显式标明用了哪条、`warnings` 说明局限。Word / 图片 OCR 未接入 —— SRS 原文列为「后续可扩展」，属规划项 |
| REQ-M1-02 | 文档解析与字段抽取 | ✅ | 9 个规范字段（name/age/degree/school/major/language/gpa/intention_country/intention_stage）；横向表先按列名还原成「标签\|值」再抽；`_LABEL_WORDS` 黑名单挡住「把表头当值」 |
| REQ-M1-03 | 规则装载 + 版本化 | 🔴 | 见第一节 |
| REQ-M1-04 | 双产品画像研判 | ⚪ | 见第三节 |
| REQ-M1-05 | 研判结论输出（命中产品/结论/依据/缺失字段/置信度） | ✅ | `conclusion` / `hit_products` / `evidence` / `confidence` / `missing_fields` 齐全；缺失字段单独算，不塞「—」占位 |
| REQ-M1-06 | 人工复核 | ✅ | `PATCH /screening/{id}/review`；`ai_conclusion`（AI 原值，写一次不改）与 `conclusion`（对外生效值）双字段留痕；`GET /screening/corrections` 输出「AI 结论 → 人工结论」迁移分布做规则优化素材 |
| REQ-M1-07 | 批量研判 | ✅ | `POST /screening/batch`，`db.begin_nested()` SAVEPOINT 隔离单条失败（冒烟用例 47/48 验证「一条失败不带走其余两条」） |

### M2 客服 Agent（SRS 4.2）

| 编号 | 需求 | 判定 | 证据 |
| --- | --- | --- | --- |
| REQ-M2-01 | 公司信息咨询 | ⚪ | 见第三节 |
| REQ-M2-02 | 公司业务查询 | ⚪ | 见第三节 |
| REQ-M2-03 | 海外留学政策查询 | ⚪ | 见第三节（另缺政策时效提示） |
| REQ-M2-04 | 课程与项目推荐 | 🟡 | `course_project` 表在，自动匹配逻辑不在 |
| REQ-M2-05 | 活动与讲座报名 | ✅ | `POST /activities/{id}/enroll` + 报名名单查询；AC-03 可端到端验证 |
| REQ-M2-06 | 常见问题自助解答 | ⚪ | 见第三节 |
| REQ-M2-07 | 日常闲聊互动 | ⚪ | 见第三节 |

### M3 企业智能助手（SRS 4.3）

| 编号 | 需求 | 判定 | 证据 |
| --- | --- | --- | --- |
| REQ-M3-01 | 意向客户录入 | 🟡 | 文字 ✅；语音只到转写+槽位，未接录入闭环、无人工校对 |
| REQ-M3-02 | 意向客户查询 | ✅ | `GET /leads`、`GET /leads/{id}`（含最近跟进） |
| REQ-M3-03 | 客户状态更新 | ✅ | `PATCH /leads/{id}/status`，审计记 `{"before": …, "after": …}` |
| REQ-M3-04 | 口述日报 | ⚪ | `POST /reports/daily` + `POST /chat/asr` 链路完整，ASR 为 mock |
| REQ-M3-05 | 管理日报查阅 | ✅ | `GET /reports/daily/summary`、`/reports/daily/trend` |
| REQ-M3-06 | 组织架构查询 | ✅ | `/org/tree`（按汇报线组装）、`/org/departments`、`/org/employees`、`/org/employees/{id}` |
| REQ-M3-07 | 新人入职指引 | ✅ | `/org/onboarding/` 下 guide / checklist / faq / contacts 四个接口；内容在 `services/onboarding.py`（版本化知识资产） |
| REQ-M3-08 | 学生业务综管 | ✅ | 成绩录入查询 `/scores`、请假审批 `/leave/apply`+`/leave/{id}/approve`、工单 `/tickets` |
| — | NL2SQL 与安全校验 | ✅ | `services/nl2sql.py`：8 个受控模板 + 17 表白名单 + 强制 SELECT + LIMIT + 超时；`GET /nl2sql/templates` |
| — | 主动待办推送 | ✅ | `services/todo.py`（待办是「算」的、推送记录是「存」的、`dedupe_key` 幂等 + 频控）；`/todo/pending`、`/todo/push`、`/todo/pushes`、`/todo/pushes/{id}/ack` |
| — | 指令式业务处理 | 🟡 | 接口与意图在（`leave_approve` 等），但 **SRS 4.3.4「执行前需向员工回显确认信息」无实现**（无确认态、无 pending 动作表） |
| — | 业务规则：跨部门数据不可见 | 🟡 | 只做到**角色级**（`staff_only`）。`GET /leads` 不带任何行级过滤条件，`employee` 可查看全部客户线索（`app/api/v1/crm.py:53`） |

### M4 学生智能助手（SRS 4.4）

| 编号 | 需求 | 判定 | 证据 |
| --- | --- | --- | --- |
| REQ-M4-01 | 行政服务（请假闭环 + 推送班主任 + 移动端审批） | ✅ | 「提交→写库→待办推送→审批→状态回写」全链路；请假接口带 `idempotency_key`。⚠️「推送班主任」= 写 `todo_push` 记录，**无真实消息通道**（企微/短信） |
| REQ-M4-02 | 心理关怀（实时监测 → 高危触发预警 → 辅助介入） | ✅ | `student_mental_profile` + `mental_alert`；预警只对 manager/admin 出现，handler 内再卡一道权限（意图路由层 employee 对 enterprise_assistant 本来就放行）。⚠️ 同理无真实触达通道 |
| REQ-M4-03 | 售后反馈 | 🟡 | 见第二节 |
| REQ-M4-04 | 学业考务 | 🔴 | 见第一节 |
| REQ-M4-05 | 进度追踪 | 🟡 | 见第二节 |
| REQ-M4-06 | 生活支持（海外生活知识库） | ⚪ | 回答来自硬编码 mock KB，无独立知识库 |
| REQ-M4-07 | 增值转化 | 🟡 | 见第二节 |
| — | 业务规则：心理数据权限隔离 | ✅ | 学生端不回显预警；`/alerts` 限管理层 |
| — | 业务规则：生活支持标注「非专业意见」 | 🟡 | 未见统一约束，需逐条确认文案 |

### M5 智能报告（SRS 4.5）

| 编号 | 需求 | 判定 | 证据 |
| --- | --- | --- | --- |
| REQ-M5-01 | 全域客户经营分析报告（周） | ✅ | `customer_ops`：意向/成交/流失三群，同环比 + 特征聚类 + 归因 |
| REQ-M5-02 | 员工日报汇总（日） | ✅ | `daily_digest` |
| REQ-M5-03 | 员工日报汇总（周） | ✅ | `weekly_digest` |
| REQ-M5-04 | 学生心理健康周报 | ✅ | `mental_weekly`（情绪标签 + 趋势 + 疏导建议） |
| REQ-M5-05 | 投诉处理周报 | ✅ | `complaint_weekly`（分类 + 时效 + 未决预警 + 满意度） |
| — | 四格式导出 | ✅ | `EXPORT_FORMATS = (xlsx, print, pdf, md)`；xlsx 与 pdf 为纯标准库自研 writer |
| — | 定时生成 | ✅ | `due_types()` 纯函数（日报类每天、周报类周一）+ `run_scheduled()`，`(type, period_start, period_end, from_schedule=True)` 幂等 |
| — | **定时推送** | 🔴 | 见第一节 |
| — | 先快照再渲染（口径一致） | ✅ | `services/reports.build()` 只跑一次聚合存进 `ReportRecord.snapshot`，四个出口只读这一份 |
| — | 口径诚实（无状态变更历史 → 用 updated_at 近似） | ✅ | 写进 `snapshot["notes"]` 并在界面/导出展示 |
| — | 在线查阅 + 下钻明细 | ✅ | `GET /reports/{id}` 含指标快照下钻 |
| — | 权限隔离与脱敏 | ✅ | 心理与投诉责任人内容做隔离 |
| — | PDF 中文渲染 | 🟡 | 已知限制：用预定义 CJK 字体不嵌字体文件，**Edge/Chrome 的 PDFium 中文渲染为乱码**（Acrobat / WPS / Foxit 正常）。已在 UI 与文档明说；界面主推 `print` 格式（浏览器另存 PDF，中文一定正确） |

---

## 五、非功能需求查验（SRS 第六章）

| 分类 | 要求 | 判定 | 说明 |
| --- | --- | --- | --- |
| 6.1 性能 | FAQ ≤1s / RAG 首字 ≤2s / 研判 ≤5s / 报告 ≤5min / 并发 ≥200 / 可用性 ≥99.5% | 🔴 | **无任何压测或性能测试证据**。冒烟 149 用例累计 825ms 只是功能连通性，不是性能指标 |
| 6.2 安全 | 全链路 HTTPS | 🔴 | 只有 HTTP；`docker-compose.yml` 无网关/TLS 终止 |
| | 敏感字段加密存储 | 🔴 | `CustomerLead.phone` 明文入库（`app/models.py:69`）。`Student.id_card_masked` 只是「脱敏值」字段命名约定，**无实际加解密实现**（全库无 encrypt/fernet/AES） |
| | RBAC + 跨部门数据隔离 | 🟡 | 角色级 RBAC ✅（`require_roles` + 角色→Agent 白名单）；**行级/部门级过滤缺失** |
| | 关键操作全量审计留痕 | ✅ | `audit_log` 含操作人、角色、动作、对象、`before/after`、IP、`trace_id`，与业务同事务提交 |
| | 内容安全过滤（双向） | 🔴 | 无输入/输出过滤、无敏感词、无承诺性话术拦截 |
| | 数据出境合规 / 对话留存销毁机制 | 🔴 | 无相关实现与文档 |
| 6.3 可维护性 | 知识库/FAQ/Prompt/研判规则后台可配置，不改代码不发版 | 🔴 | 无管理后台配置面；只有 `POST/GET /kb/documents` 的知识文档**元数据**；Prompt、FAQ、研判规则均无配置入口 |
| | 组件可插拔（模型/向量库/ASR） | 🟡 | ASR 有 `AsrProvider` 协议、Dify 有 mock/live 双模；**模型主备路由、向量库替换未实现** |
| | 运营指标看板（命中率/意图准确率/耗时） | 🔴 | 无。仅有结构化日志 + `trace_id` + `x-process-time-ms` 响应头 |
| 6.4 兼容性 | 主流浏览器 / 移动端 H5 | ✅ | 零构建 ES Module 前端 + 静态服务；36 项浏览器 E2E 全过（本机 Edge）。移动端窄屏适配需人工确认 |
| | 公众号 / 企业微信 / 小程序渠道 | 🔴 | 未接入。`chat` 接口有 `channel` 字段，但无渠道适配层、无回调入口（`webhook` 全库 0 命中） |
| | 容器化部署 | ✅ | `Dockerfile`（python:3.12-slim + HEALTHCHECK + 双 worker）+ `docker-compose.yml`（MySQL 8 + api） |
| | K8s 扩展 | 🔴 | 未做（TDD 列为二期，可接受） |
| 6.5 易用性 | 对话式交互为主 | ✅ | 统一对话入口 + 意图路由可视化 |
| | 操作类功能明确确认与**撤销**机制 | 🔴 | 无确认态、无撤销（全库 `撤销`/`undo` 0 命中） |

---

## 六、验收标准查验（SRS 第七章）

| 编号 | 验收项 | 判定 | 说明 |
| --- | --- | --- | --- |
| AC-01 | 研判准确率 ≥90% 且结论可追溯 | 🔴 | **无甲方样本集**、无准确率实测。结论来自 mock 占位实现，「可追溯到规则条目」因 REQ-M1-03 未落地而不成立 |
| AC-02 | 7 类意图准确率 ≥90%、问答抽样合格率 ≥95% | 🔴 | 无标注集、无准确率实测 |
| AC-03 | 活动报名闭环，后台可查到完整记录 | ✅ | 冒烟用例覆盖（报名 + 名单查询） |
| AC-04 | 8 项功能全部可用；NL2SQL 正确率 ≥90% | 🟡 | 8 项功能可用 ✅；NL2SQL 仅有单元测试，**无「约定问题集」正确率实测** |
| AC-05 | 指令式操作正确执行并留痕，误操作率 <1% | 🟡 | 留痕 ✅；二次确认缺失；误操作率无实测 |
| AC-06 | 7 类场景全部可用；请假与投诉闭环端到端跑通 | 🟡 | 请假/投诉闭环 ✅；7 类中「学业考务」🔴、「进度追踪/增值转化」🟡 |
| AC-07 | 高危样本全部触发、触达时延 ≤5s、无漏报 | 🔴 | 无真实高危样本集；**无触达通道**，时延无法测量 |
| AC-08 | 5 类按时自动生成 + 四格式导出 + 定时推送 | 🟡 | 生成 ✅ / 导出 ✅ / **定时推送 🔴** |
| AC-09 | 性能达标 + 渗透测试无高危漏洞 | 🔴 | 未做 |

---

## 七、与《技术方案设计文档》的偏差（19 处，按严重度排序）

| # | TDD 章节 | 设计要求 | 实际 | 级别 |
| --- | --- | --- | --- | --- |
| 1 | 4.5 工具调用 | 工具表 **19 个**（含 `lead_create` `leave_apply` `leave_approve` `ticket_create` `ticket_update` `activity_enroll` `screening_analyze` `screening_batch` `screening_review` `material_upload` `org_query` `onboarding_query` `todo_push` `report_generate` `report_daily_submit` `progress_query` `lead_update_status` 等写类工具） | `/internal/tools` 只暴露 **4 个**（`lead_lookup` `student_scores` `pending_requests` `nl2sql`），**且全部只读**；命名也对不上（`lead_query`→`lead_lookup`、`nl2sql_query`→`nl2sql`）。写类能力只能走 REST，Dify Agent **无法执行任何写操作**——「指令式业务处理」在 live 模式下走不通 | 🔴 高 |
| 2 | 4.3 会话状态与记忆 | Redis 短期记忆（最近 N 轮 + 槽位状态，30 分钟）+ 多轮填槽 | 无 Redis、无槽位状态机；多轮靠 Dify `conversation_id` | 🔴 高 |
| 3 | 4.4 Prompt 工程 | 三层分层拼装 + 版本化 + 灰度回滚 + 拒答/引用强制 | 无 Prompt 管理层（全在 Dify 侧，当前未接线） | 🔴 高 |
| 4 | 5.3 检索策略 | 向量 Top20 + BM25 Top20 → Rerank → Top5 → 阈值拒答 | 无向量库/embedding/rerank | 🔴 高 |
| 5 | 5.4 幻觉抑制 | 低温、FAQ 精确匹配优先、输出前事实一致性检查 | 无 | 🔴 高 |
| 6 | 3.2 核心链路 | 「操作类 → 工具调用 → **二次确认** → 写库 → 回执」 | 写库与回执有，二次确认无 | 🔴 高 |
| 7 | 9.1 安全 | 双向内容安全过滤、字段级加密、HTTPS | 均无 | 🔴 高 |
| 8 | 9.2 性能 | Redis 缓存 FAQ、限流熔断、检索并行化 | 均无 | 🔴 高 |
| 9 | 9.3 可观测性 | Prometheus + Grafana + Loki、告警规则、运营看板 | 只有结构化日志 + `trace_id` | 🔴 中 |
| 10 | 9.4 可用性容灾 | 多实例 + LB、DB 主从 + 备份、模型多供应商主备 | 单实例 compose（`--workers 2`） | 🔴 中 |
| 11 | 7.4 外部对接 | 教务、CRM/申请系统、HR/组织架构、企业微信、学生端渠道 5 类对接 | **0 类实现**（`webhook` 全库 0 命中） | 🔴 高 |
| 12 | 10.1 环境划分 | DEV / TEST / UAT / PROD 四套环境 | 只有一个 `DEBUG` 开关 | 🔴 中 |
| 13 | 10.3 上线与发布 | CI/CD（提交→静态检查→单测→构建镜像→自动部署）、滚动发布、一键回滚 | 无 `.github/`，无 CI 配置 | 🔴 中 |
| 14 | 3.1 渠道层 | 官网客服窗口、公众号、企业微信、小程序、管理后台多端入口 | 只有自研 Web 管理台；渠道仅存 `channel` 字段 | 🔴 中 |
| 15 | 4.2 路由阈值 | confidence ≥0.75 分派 / 0.5~0.75 澄清 / <0.5 兜底转人工 | 无分档澄清逻辑，靠「最长关键词优先」打分 + 角色降级 | 🟡 中 |
| 16 | 6.1 数据模型 | 19 张表 | 19 张表，**一致** ✅ | — |
| 17 | 7.2 接口清单 | 64 路径 / 70 操作 / 9 组 | 实测 **64 路径 / 70 操作 / 9 组**，**一致** ✅ | — |
| 18 | 4.1 Agent 划分 | 6 个 Agent | `ROLE_AGENTS` 中 6 个（customer_service / student_helper / enterprise_assistant / mental_care / reporter / screener），**一致** ✅ | — |
| 19 | 8.5 报告生成 | 生成 → 写 `report_record` → **按配置推送至对应角色** | 推送到角色的环节缺失 | 🔴 中 |

---

## 八、需要甲方配合或部署环境才能判定的事项

| # | 事项 | 卡在哪 |
| --- | --- | --- |
| 1 | 《用户画像研判规则》文件 + 两个产品的定义与判定阈值 | SRS「待确认事项」第 1、2 条，甲方未提供 → M1 核心判定无法实现 |
| 2 | 《公司信息》《公司业务》《留学政策》三份文档 + 历史高频咨询记录 | 知识库内容源缺失 → M2 全部场景只能跑 mock |
| 3 | 教务系统、CRM 系统接口文档与测试账号 | → REQ-M4-04 / M4-05 无法实现 |
| 4 | 组织架构与员工数据来源 | 现为 seed 假数据 |
| 5 | 对话渠道范围与开放平台账号 | → 渠道层无法落地 |
| 6 | 大模型选型与数据出境合规意见 | → 影响 live 模式能否启用 |
| 7 | 学生心理数据查看权限与留存期限（内部制度） | → 影响 M4-02 与 M5-04 的合规口径 |
| 8 | 报告的推送对象、渠道、时间 | → 报告定时推送的配置依据 |
| 9 | 语音识别是否需方言、ASR 由谁提供 | → ASR 接线方案 |
| 10 | 完整开发计划、优先级与人员分工 | 现仅有客户需求表「开发计划」页的 2 条（意向客户表设计与接口开发 ✅、企业智能助手-意向客户意图开发 ✅，**两条均已落地**） |

---

## 九、已做且本次实测可复验的证据

| 项目 | 本次实测值 | 复验命令 |
| --- | --- | --- |
| 单元/集成测试 | **320 / 320 通过**（2026-09-15 19:47 实跑） | `pytest -q` |
| 一键自检 | **13 / 13 通过**（起服→体检→收服，19:48 实跑） | `python scripts/launcher.py --selfcheck` |
| HTTP 冒烟 | **149 / 149 通过**，累计 825ms（19:48 实跑，报告已刷新） | `start.bat --smoke` |
| 浏览器 E2E | **36 / 36 通过**，0 页面异常（今日 14:57） | `node scripts/frontend_e2e.mjs` |
| 接口面 | 64 路径 / 70 操作 / 9 组，与文档一致 | `GET /openapi.json` |
| 数据表 | 19 张，与文档一致 | `app/models.py` |
| 前端导入一致性 | OK | `python scripts/check_frontend_imports.py` |

---

## 十、建议的补齐优先级

**P0（不做则核心验收过不了）**
1. 甲方索取《用户画像研判规则》→ 落成可版本化的规则引擎（REQ-M1-03 / AC-01）
2. 补齐 Dify 侧的 6 个应用与知识库，把 `DIFY_MODE` 切到 live，做一次真链路回归（AC-02 / 全部 M2）
3. 把 TDD 4.5 的工具表补齐并接线，至少覆盖写类工具的二次确认（AC-05 / TDD 偏差 #1、#6）

**P1（面向生产可用）**
4. 报告定时推送（AC-08）
5. 真实触达通道（企业微信 / 短信）——否则 AC-07 永远无法验收
6. 字段级加密 + HTTPS + 内容安全过滤（6.2）
7. 限流熔断 + 基础监控探针（6.1 / 9.2 / 9.3）

**P2（完善度）**
8. 学业考务数据源与考前提醒（REQ-M4-04）
9. 撤销机制与写操作确认（6.5）
10. 行级/跨部门数据隔离（SRS 4.3.4）
11. 性能压测与渗透测试（AC-09）
