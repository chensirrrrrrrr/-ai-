# 验收基线汇总（ai-assistant）

- **项目**：留学机构 AI 智能助手系统
- **项目根（主目录）**：`C:\Users\机械革命\Desktop\留学机构AI助手系统_交付\02-源代码\ai-assistant\`
  （原 `2026-09-12-11-04-16\` 已不存在）
- **汇总时间**：2026-09-15 20:3x ｜ **最近复核**：2026-09-17（死代码清理 + 补测后全量重跑）

> 本文是资料库的**入口文档**：先看这里了解当前验收状态，再按需下钻到具体报告。

---

## 一、四条验证基线（2026-09-17 复核）

| 验证项 | 结果 | 说明 | 明细文件 |
| --- | --- | --- | --- |
| 单元/集成测试（pytest） | **502 / 502**（覆盖率 **93%**） | 覆盖 9 组、19 张表相关业务逻辑（含端口契约 8 条 + 调度/补列/工具守卫/通知外发/改密/ASR 分支） | 无独立文件（命令行输出） |
| 一键自检（--selfcheck） | **13 / 13** | 依赖、配置、数据库、端口、探活、静态资源 | 无独立文件（命令行输出） |
| 接口冒烟测试 | **149 / 149** | 真实起服务后发请求，覆盖健康/认证/对话/权限/业务链路 | `smoke_report.md` |
| 浏览器端到端（E2E） | **37 / 37** | 真实浏览器驱动，逐页截图 | `frontend_e2e_report.md` / `.json` |
| Dify 接线预检 | **7 / 7 [OK]** | 7 个应用 Key 逐个探活 | 无独立文件（`scripts/check_dify.py`） |

**一条命令复现全部基线**：

```bash
python scripts/launcher.py --selfcheck --e2e
```

> 注意：E2E 必须在本进程内跑（自检流程会在服务起来后同进程执行 E2E，跑完再收服务）。
> 不要用后台方式挂起 launcher —— stdout 管道不收口会假死，服务也会被连带杀掉。

### 端口约定（🔴 以 `scripts/launcher.py` 为唯一权威源）

后端 **8010** / 前端 **8020**（`DEFAULT_BACKEND_PORT` / `DEFAULT_FRONTEND_PORT`）。

```bash
python scripts/launcher.py --headless --no-browser   # 起服（8010 / 8020）
python scripts/launcher.py --status                  # 查状态
python scripts/smoke_test.py                         # 冒烟：--base 默认自动发现
python scripts/launcher.py --stop                    # 停服
```

⚠️ `smoke_test.py` 的 `--base` 默认值曾硬编码 `8000`，与 launcher 的 8010 漂移 ——
手工跑冒烟会全部打空。已修为自动发现（`--base` 显式传参 → `.run/pids.json` 的
`backend_port` → launcher 默认端口），并由 `tests/test_port_contract.py`（8 条）钉住。

---

## 二、文件清单

| 文件 | 内容 | 建议读者 |
| --- | --- | --- |
| `需求完成情况查验报告.html` | **主报告**（自包含 HTML，可直接打开/打印）。含首次查验结论 + 顶部「修复进展」复核表 | 全员 |
| `需求完成情况查验报告.md` | 同上，Markdown 源文件（便于二次编辑） | 产品 / 文档维护者 |
| `smoke_report.md` | 149 条接口冒烟用例逐条结果 + 耗时 | 后端 / 测试 |
| `frontend_e2e_report.md` | 37 条端到端用例逐条结果 | 前端 / 测试 |
| `frontend_e2e_report.json` | 同上，机器可读格式（含控制台错误、失败请求明细） | 前端 |
| `frontend_shots/` | E2E 各步骤截图 | 全员（看效果） |

---

## 三、需求完成度速览

功能需求 **34 条**（M1–M5）：

| 判定 | 条数 | 含义 |
| --- | --- | --- |
| ✅ 已实现 | 22 | 含本次新闭环的 3 条（规则引擎 / 学业考务 / 报告定时推送） |
| 🟡 部分实现 | 5 | 主链路在，关键环节缺失或口径收窄 |
| ⚪ 依赖外部未接线 | 7 | 代码结构就位，真实能力挂 Dify / ASR / 教务 CRM，当前 mock |
| 🔴 未实现 | 0 | 首次查验的 3 条🔴已全部闭环 |

---

## 四、本次闭环的空档（2026-09-15）

| 原空档 | 修复落点 |
| --- | --- |
| 🔴 REQ-M1-03 画像研判规则导入与版本化 | 新增 `app/services/rules.py`，替换 Dify 硬编码占位 |
| 🔴 REQ-M4-04 学业考务（DDL / 考试时间 + 考前提醒） | 新增 `app/services/deadline.py`，挂进 `scheduler.tick()` |
| 🔴 报告定时推送（SRS 4.5.3 / AC-08） | `reports.run_scheduled_with_push()` 打通生成→分发 |
| ⚪ 通知渠道缺失 | `app/services/notify.py` 统一渠道 + 降级诚实上报 |
| TDD 4.5 工具表 19 个工具，实际仅 4 个只读 | 新增 `app/services/agent_tools.py`：**26 个工具（读 12 + 写 14）** + 写操作两阶段确认 |
| 老库缺新表/新列 | 新增 `scripts/sync_schema.py`（只加不删），接入 launcher |

---

## 五、未闭环项（已立项跟踪）

均已建为项目事项，分三组：

- **P0 上线阻塞**（urgent）：甲方规则文件索取 · Dify 切 live 真链路回归 · 19 工具 Dify 侧联调验收
- **P1 面向生产可用**（high）：真实触达通道 · 加密+HTTPS+内容安全 · 限流熔断+监控告警
- **P2 完善度与工程化**（medium/low）：行级数据隔离 · 撤销机制 · 压测+渗透 · Redis/向量库/Prompt 版本化 · CI/CD

> 详细的缺口证据与影响分析见 `需求完成情况查验报告` 正文。

---

## 六、2026-09-16 进展：Dify 切 live 并收口静默降级

### 1. Dify 真实链路打通（本机自建 Dify 1.10.0）

7 个应用全部接活：4 个 chat（客服 / 学生助手 / 企业助手 / 心理关怀）+ 3 个 workflow
（意图路由 / 画像研判 / 报告生成）。`.env` 已置 `DIFY_MODE=live`，
API Key 已整轮轮换（7 个全换、旧 Key 全删）。`check_dify.py` **7/7 [OK]**。

### 2. 知识库就绪，知识库引用从 `refs=0` 修到 `refs=4`

知识库 `留学机构业务知识库`：26 篇文档全部 `completed`、100 条向量，
索引 `high_quality` + `embedding-3`（智谱云端），端到端答复能指回具体文档。

**根因**（值得记住）：`model-config` 里 `dataset_configs.datasets.datasets[].dataset`
必须带 `"enabled": true`，否则 Dify 的 `DatasetConfigManager.convert()` 会静默跳过该数据集，
`dataset_ids` 为空 → 检索整条链路不生效、`references` 恒为空。修好即 `refs=4`。

### 3. 修掉两处「静默降级」（都会让结论不可信）

| 现象 | 根因 | 现状 |
| --- | --- | --- |
| 画像研判结论抖动（`符合/0.86` ↔ `信息不足/0.4`） | `screening_rule` 表 0 行 → `rule_source=dify`，结论全交给 LLM | 已补幂等守卫回填 2 条 ACTIVE 规则；`rule_source=local`，连续 5/5 确定 |
| 意图路由 `intent` 返回整句中文 | router workflow 的 schema `description` 写的是「一句话概括用户意图」，模型照字面执行 | 已给 `intent` 加 17 值 enum + 提示词词表；6/6 返回受控短标识 |

### 4. 启动方式统一到 launcher，端口不再漂移

服务统一由 `scripts/launcher.py` 管理（8010 / 8020），
`smoke_test.py` 的端口改为自动发现并加契约测试守护（见上文「端口约定」）。

### 复测基线

pytest **502 / 502** ｜ selfcheck **13 / 13** ｜ 冒烟 **149 / 149** ｜ E2E **37 / 37** ｜ Dify **7 / 7 [OK]**

> 期间还清掉了一处隐患：`scripts/seed.py` 里规则播种漏了判空守卫，导致老库永不回填 ——
> 该脚本只在数据库不存在时自动执行，**后续加内容必须自带判空守卫**。

---

## 七、2026-09-17 进展：交付文档 vs 代码「三查」+ 清理整改

以交付文档 `AI智能助手系统_技术实现与部署文档.docx` 为基准做了一次实现侧自证
（完整性 / 冗余 / 缺陷），报告见 `代码实现与测试查验报告.md`。

| 结论 | 整改 |
| --- | --- |
| 文档登记 70 个接口 **100% 有实现**；真实 API 为 **82 路径 / 91 操作**（10 组）⇒ 文档接口表滞后 21 条 | 已回填：md 源 + docx 接口清单表（新增「通知」组）+ stage2 HTML + 附录 B 接口总数 |
| `response_model` 全项目 0 次 ⇒ 24 个 `*Out` 疑似死代码 | 经**传递可达性**复核为 **20 个**真死（`RuleItem`/`RuleProduct`/`DeadlineItem`/`ProgressItem` 在 schemas 内部被引用，保留），已删除；`schemas.py` 51 → 31 个模型 |
| 未使用导入 3 处 / 未被调用函数 1 个 | 3 处导入已删；`agent_tools.cancel()` **不是**冗余而是未接线 —— 代码里 `CANCELLED` 状态与 `confirm()` 的错误分支都已就绪，故**补上撤回通道**（`{"cancel": token}`，不加新路由），并补 4 条用例 |
| 根目录 `_probe.py` 与 `_lab/`（13 MB）为临时物 | 已移出交付包至 `C:/Users/Public/uas_doc/ai_assistant_moved_out_20260917/`（移动，非删除） |
| 低覆盖模块：scheduler 64% / db 65% / agent_tools 67% | 补测后：**scheduler 100%** / **db 96%** / **agent_tools 78%**；整体 **88% → 90%** |
| 剩余非阻塞低覆盖：notify 73% / system 77% / asr 80% | 同日续做收口：三个模块**均补到 100%**；整体 **90% → 91%**（+32 条用例，见下节） |
| 最后一个低于 90% 的服务层模块：`agent_tools.py` 78%（116 行未覆盖） | 同日再收口：补读工具过滤分支 + 写工具成功路径 + 注册表自检 + 参数收敛，共 **30 条** → **100%**；整体 **91% → 93%**（见下节） |

**复测基线**：pytest **502 / 502**（**93%**）｜ 冒烟 **149 / 149** ｜ E2E **37 / 37**。

> 回归方式：清理前先整体打包 `C:/Users/Public/uas_doc/ai_assistant_pre_cleanup_20260917.zip`（412 文件 / 6.2 MB）。

### 7.1 低覆盖模块收口（同日续做）

| 新增 / 追加 | 条数 | 锁住的分支 |
| --- | --- | --- |
| `tests/test_notify_delivery.py`（新） | 15 | `_post_json` 2xx/非 2xx/抛异常；`_deliver` 四通道载荷；`send` 外发成功不降级、**外发失败兜底落站内且保留原因**；标题截断/空标题；`mark_read(read=False)` |
| `tests/test_asr.py`（新） | 12 | Provider 选择（dify 大小写不敏感 / **未知厂商显式拒绝，不静默退 mock**）；Dify 转写成功、`UpstreamError` 透传、未知异常包装；槽位抽取 |
| `tests/test_system.py`（追加） | +5 | 健康探针 **DB 不可用 → degraded**；停用账号 403；改密全链路（太短 400 / 原密码错 401 / 生效）+ 审计留痕 |

三步收口值不值得做，看的是「失败路径」——这三处恰好全是**失败路径**：
通知发不出去、数据库连不上、上游 ASR 报错。任何一条没被试过，线上就只剩「看起来正常」。

### 7.2 工具层分支收口（同日再续）

`app/services/agent_tools.py` 收口前为 525 语句 / 116 行未覆盖 / **78%**，是服务层最后一个
低于 90% 的模块，也是唯一会**改业务数据**的入口。新增 `tests/test_agent_tools_coverage.py`（**30 条**）：

| 类别 | 补的内容 |
| --- | --- |
| 读工具过滤分支 | `lead_query` 的 status / phone / owner（ID + 姓名连表两条路）、`student_scores` 按名、`my_tickets` 按状态、`org_query` 按关键字、`onboarding_query` 的 checklist / contacts |
| 写工具成功路径 | `lead_update_status`、`ticket_update`（通知 + `resolved_at` + `handler_id`）、`activity_enroll` 六道闸、`todo_push`、`report_generate` |
| 注册表自检 / 参数收敛 | `_validate_registry` 三个 `RuntimeError`、`_coerce` 的 bool/array/报错分支、`confirm` 的「token 不存在」「工具已下线」 |

结果：`agent_tools.py` **78% → 100%**；全量 **502 / 502（93%）**；一键回归
（pytest + 起服体检 + HTTP 冒烟 + 浏览器 E2E）**23 项全过**。除 §7.1 那一处失败隔离修复外未改其它 `app/` 代码。

> 收口时发现一处不一致（`_screening_batch` 的 `lead_ids` 拼文本在 `try` 之外，单条客户不存在会炸整批），
> **已经用户确认后修复**（详见查验报告 §7.1）：`lead_ids` 与 `items` 现在一视同仁地逐条隔离失败，
> 修复后全量回归重跑全绿。
